"""Shared fixtures. Unit tests use tiny models from here — never
instantiate a model inline in a test (CLAUDE.md, "Fixtures").

Empty for now: no fixture exists until models.py and lora.py have
enough to build one. Add fixtures here as the tiny-model test needs
them, not preemptively.
"""
