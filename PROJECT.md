# Induction Bandwidth

**What is the minimum-dimension weight update that installs a working induction circuit into a pre-induction Pythia checkpoint, and does the update's symmetry structure differ between the circuit's prefix-matching and copying halves?**

This is the living specification and the source of truth. It is updated at the end of every working session. If a claim here contradicts the code, one of them is wrong — resolve it explicitly in §10 and never let both stand.

---

## Status board

| Phase | Item | Status | Notes |
|---|---|---|---|
| Setup | `pyproject.toml`, package skeleton, CI green on empty suite | ● | CI run 31238848784 green on commit 1af4d12: tier0/1/2/2.5 all pass, tier3 correctly skipped (workflow_dispatch only) |
| Setup | `algebra.py` + unit tests against closed-form oracles | ● | §3 identities are exact; test to machine precision. 23 tests (unit oracles + property invariants), all pass at rtol=1e-12 where exact |
| Setup | `probes.py` + silent-failure guards | ● | PMS, prev-token score, copying score, ICL score, recovery R implemented as pure functions over tensors (no model loading — see §10). 29 tests (unit oracles/discrimination + property invariants), all pass |
| Setup | `models.py` Pythia loader + local checkpoint cache | ● | `load_checkpoint(step)` validated against a real download (pythia-70m, step 0): architecture matches §2 exactly (6 layers, d_model=512, 8 heads, d_head=64), forward pass finite. 4 unit tests (mocked, no network) + 2 integration tests, all pass |
| Setup | `nulls.py` matched-norm / matched-β controls + φ null bands | ● | PROJECT.md §8's mandatory controls, as pure array functions (same pattern as algebra.py/probes.py/lora.py). `matched_norm_random_update`, `phi_null_band`, `phi_separation_null_band`, `matched_beta_support`; `bandwidth`/`bandwidth_fraction` in lora.py. 33 tests. Two operationalizations flagged in REVIEW.md, not assumed |
| Setup | Tier-0 metric-hash gate + tier-2 smoke test | ● | Both were specified in CLAUDE.md and unimplemented — `ci.yml` carried the metric-hash step as an `echo` stub and tier 2 ran no smoke test. `src/indbw/metric_hash.py` + `metric_hash.json` + `scripts/check_metric_hash.py` (wired into CI tier 0 and pre-commit); `tests/smoke/test_smoke.py` runs the full path on `tiny_model` in ~4s. 17 + 1 tests |
| Setup | `lora.py` three parameterizations + rank/parity tests | ● | Pure functions over plain arrays (same pattern as algebra.py/probes.py), no model coupling. unconstrained: standard `(alpha/r) B A` on any rectangular shape. symmetric/antisymmetric: only defined on a square delta, built from `rank // 2` vector pairs — `rank` must be even (parity theorem). 27 tests (unit oracles/discrimination + property invariants), all pass |
| G0 | Induction transition located at 70m, bracket width measured | ● | **Gate passed.** 7-way worker sweep completed 154/154 checkpoints (data/g0_sweep_worker{0-6}.jsonl); scripts/close_g0.py merged the shards, ran `locate_transition`, and emitted results/g0_transition.jsonl. **A = step 512, B = step 2000, bracket_width = 2** (≤ 3 threshold). N_eval=512, T=128, seed=0 per §5 |
| G1 | Prerequisite check at A: prev-token head, $W_K$ overlap | ● | **Gate passed.** At A (step 512): previous-token head found at layer 2, head 1 (score 0.356 ≥ 0.3). Candidate induction head (layer 3, head 6, identified from B's PMS) has $\|P_{\text{prev-tok}}W_K\|_F/\|W_K\|_F = 0.387$ at A, vs. a random-subspace-null 95th percentile of 0.360 — significant, but the margin (0.027) is narrow. results/g1_prerequisite.jsonl |
| G2 | CPU wall-clock for one run to criterion at 70m | ● | **Gate closed: CPU affordable.** 400 steps (max_steps, criterion not reached) in 2198s (5.5s/step) on checkpoint A, r=64 QK-only. Projected full 108-cell sweep on a 7-way worker fleet: 9.8h (≤ 24h threshold) → **CPU**. Two earlier attempts on this box were invalidated by memory exhaustion, not genuine compute cost (§10, §11) — real per-step compute is ~3.7s (directly profiled) |
| G3 | Positive control: generous rank on $W_Q$ alone reaches criterion | ✗ | **Gate failed**, and now diagnosed as far as an agent is permitted to take it. Eight training/optimization diagnostics (§10, 2026-08-14 through 2026-09-03) rule out lr, rank, frozen-$W_K$, target head, full fine-tune, and step budget; the reachability graft shows B's own $W_Q$ for the target head does not move $R$ ($-0.0006$), which upper-bounds every trained $\Delta W_Q$. The prereq A/B contrast supplies a candidate mechanism (layer 2's prerequisite is 2.6–15× weaker at $A$ than at $B$). A ninth diagnostic (2026-09-05, localization bisection) sharpens this further: grafting blocks 0-2 restores prefix matching almost fully (PMS 0.895) while $R$ stays at 0.10, even with the target head's real copying weights already in place — matching and recovery now dissociate directly, not just by argument. **No result carries a verdict** — whether this means the composition rule is too restrictive, the checkpoints aren't in the same basin, or something else, is a human call (CLAUDE.md §9). A proposed training-free next phase is in §11, **not yet executable**: its precondition (a graftable component set that reaches criterion) hasn't been met by anything tried so far, including the localization bisection. Phase stays stopped; M1–M4 do not start until a human reads §10/§11 and decides |
| M1 | QK rank sweep (adapt $W_Q$ only) | ○ | H1. Infrastructure built: `indbw.sweep` (cell config-as-data, run-ID hash, resumability, control gate) + `scripts/run_sweep.py --arm QK`, resumable and shardable across a worker fleet. The runner refuses to start until all four gates have a passing, self-consistent record — **currently blocked by G3's failure above**, not just by G2/G3 being unrun |
| M2 | OV rank sweep (adapt $W_O$ only) | ○ | H1, H3. Same runner, `--arm OV`. Same block as M1 |
| M3 | Matched-β parameterization triad (unconstrained / sym / antisym) | ○ | H2 |
| M4 | Antisymmetric fraction φ for both arms | ○ | H2 |
| M5 | SVD truncation probe on the generous-rank solution | ○ | §7 minimality |
| M6 | Matched-norm random-update nulls | ○ | h0₃, h0₄ |
| M7 | 10×-steps arm on every failing rank cell | ○ | h0₂ — mandatory before any failure claim. Now enforced, not remembered: `sweep.check_failure_claims` runs in tier 2.5 over every record in `results/`, and a capacity failure recorded without its controls fails the build. `run_sweep.py --controls` enqueues exactly the arms that are owed |
| M8 | Held-out natural-text loss, before/after | ○ | collateral damage |
| D1 | Principal angles vs pretraining delta, winning arm only | ○ | **Deferred scope — single instance, not a sweep** |

Legend: ○ not started · ◐ in progress · ● done · ✗ failed/abandoned

---

## 1. Framing

### 1.1 What we are not doing

We are not reproducing the weight delta $W(B) - W(A)$. Pythia's public grid is 154 checkpoints — step 0, log-spaced to 512, then every 1000 steps. Induction onset sits near the seam. Any available A/B pair spans billions of tokens of general LM improvement, of which the induction circuit is a small fraction. A rank measured on that delta measures the wrong object.

We are also not measuring information flow. LoRA leaves activations full-rank; every token still passes through the full $d_{\text{model}}$. What rank constrains is the dimension of the *change to the operator*. The correct name for the measured quantity is the **intrinsic dimension of the update**, not bandwidth or channel capacity. The project title is a legacy label; the claims must use the precise term.

### 1.2 What we are doing

Define the target behaviorally. Train a rank-limited update from checkpoint $A$ whose objective is to install induction behavior. $B$ is used only as the reference ceiling for the recovery metric and — in deferred scope only — as the comparison object for subspace identity.

### 1.3 Stated up front

An update trained from $A$ installs *an* induction circuit, not necessarily $B$'s. Everything here is a claim about the loss landscape near $A$, not about the pretraining trajectory. Only D1 bridges to a developmental claim, and only if it comes back positive. **Until D1 runs and passes, every result is phrased as reachability.** Developmental language in an abstract backed only by M1–M8 is the failure mode this paragraph exists to prevent.

---

## 2. Substrate

**Model: `EleutherAI/pythia-70m`.** 6 layers, $d_{\text{model}} = 512$, 8 heads, $d_{\text{head}} = 64$. Chosen over 14m because induction formation at 6 layers / 4 heads / $d=128$ is not reliably documented and the substrate risk is not worth the speed. Seed variants exist at 70m if a cross-seed band is ever needed.

410m is out of scope for training on CPU: ~30× the per-step cost and ~1.6 GB of fp32 weights before optimizer state. Available for forward-only probe sweeps if a scale check is wanted; not for any sweep.

**Checkpoint definitions.**
- $A$ — last checkpoint before the transition. No head above PMS 0.1; ICL score at baseline.
- $B$ — first checkpoint after the transition stabilizes: ICL within 10% of its value two checkpoints later.

Both selected by behavioral probe. **LLC / SGLD is out of scope entirely** — we know which transition we are targeting, so the detection machinery is unnecessary. This is a deliberate narrowing, logged in §10.

---

## 3. The algebra

Full derivation in `docs/mathematics.md`. The load-bearing results:

**The two square forms.** $M_{QK} = W_Q W_K^\top$ and $M_{OV} = W_O^\top W_V^\top$, both $d \times d$, both rank $\le d_{\text{head}}$. Individual $W_Q, W_K, W_V, W_O$ are rectangular; symmetry is undefined for them, so all symmetry claims are about the composed forms only.

**Decomposition.** $M = S + \Lambda$ with $S = \frac12(M + M^\top)$, $\Lambda = \frac12(M - M^\top)$, Frobenius-orthogonal. The antisymmetric fraction:

$$\varphi(M) = \frac{\|\Lambda\|_F^2}{\|M\|_F^2} = \frac12\left(1 - \frac{\operatorname{tr}(M^2)}{\|M\|_F^2}\right)$$

**Interpretation.** $\text{score}(i,j) - \text{score}(j,i) = \frac{2}{\sqrt{d_h}} x_i^\top \Lambda x_j$. The symmetric part is order-blind — it implements similarity. Only the antisymmetric part carries directional structure.

**The ceiling result.** If $M$ is supported on an off-diagonal block ($M = P_U M P_V$, $U \perp V$) then $\operatorname{tr}(M^2) = 0$ and $\varphi = 1/2$ **exactly, at any rank**. Prefix matching is exactly this shape. So the prediction is $\varphi \approx 1/2$, not $\varphi \to 1$.

**Rank-1 is not barred.** $\varphi(ba^\top) = \frac12(1 - c^2)$ with $c = \hat a \cdot \hat b$, maximized at $1/2$ when $a \perp b$. A single relational term $uv^\top$ is cleanly rank-1. It is the *constrained* parameterizations that force companion terms: pure-antisym gives $uv^\top - vu^\top$, pure-sym gives $uv^\top + vu^\top$.

**What sets required rank.** The target operator is $M_{QK}^\star \approx \lambda \sum_t e_t p_t^\top = \lambda E P^\top$, so required rank is $\operatorname{rank}(EP^\top)$ — the dimension of the token code the match must resolve. Default expectation is a smooth $R(r)$ tracking that spectrum. A sharp low-rank threshold would be the surprising result.

**Composition rule.** Adapting both $W_Q$ and $W_K$ gives $\Delta M_{QK}$ of rank up to $3r$ with no parity structure. **Adapt exactly one matrix per form**: $W_Q$ for QK, $W_O$ for OV.

**Existence versus reachability.** `docs/mathematics.md` §12–§18 extends the algebra to a question §3 does not otherwise ask: whether a rank-limited update installing the behaviour *exists*, separately from whether training finds one. Load-bearing there: the rank constraint is vacuous at $r \ge d_h = 64$ (Lemma 13.2); the existence question is an exactly-decidable linear program on frozen activations rather than a training outcome (§16); and the QK gradient vanishes identically when the head's OV write is position-independent, at every rank (§17). Derivation only — it adds no hypothesis and changes no threshold; see §11 for what it implies about existing rows.

---

## 4. The circuit, split

Three components across two layers. A lumped metric cannot tell you which was the bottleneck.

| # | Component | Circuit | Role here | Metric |
|---|---|---|---|---|
| 1 | Previous-token head | QK, positional | **Prerequisite, not a target** | prev-token score: mean attn $t \to t-1$ |
| 2 | Prefix matching | QK | Arm 1: adapt $W_Q$ only | PMS: mean attn $t \to$ successor of prior occurrence |
| 3 | Copying | OV | Arm 2: adapt $W_O$ only | copying score: fraction of $t$ with $\arg\max W_U M_{OV} e_t = t$ |

Report $r^\ast_{QK}$ and $r^\ast_{OV}$ separately. They have no reason to coincide.

---

## 5. Metrics

Fixed evaluation set, fixed seed. Any change to a definition invalidates prior results and **must** bump `METRIC_VERSION` in `schema.py`. CI enforces this (§9).

**Repeated-random sequences.** $T = 128$ tokens sampled uniformly from vocab, concatenated to length $2T$. $N_{\text{eval}} = 512$.

**Prefix-matching score (PMS), per head.** Mean attention from position $t$ to $t - T + 1$, averaged over $t$ in the second copy. Induction-bearing at PMS $\ge 0.3$.

**Copying score, per head.** Fraction of vocabulary tokens $t$ for which $\arg\max_s (W_U M_{OV} e_t)_s = t$.

**ICL score.** $\text{NLL}_{\text{first copy}} - \text{NLL}_{\text{second copy}}$. Higher is stronger.

**Recovery fraction.** $R(X) = \dfrac{\text{ICL}(X) - \text{ICL}(A)}{\text{ICL}(B) - \text{ICL}(A)}$. $R(A) = 0$, $R(B) = 1$ by construction. Clamped for reporting, unclamped logged.

**Bandwidth.** $\beta = r(d_{\text{in}} + d_{\text{out}})$, reported absolutely and as a fraction of the adapted matrix's parameters.

**Antisymmetric fraction.** $\varphi$ as in §3, computed on $\Delta M_{QK}$ and $\Delta M_{OV}$.

**Prerequisite overlap.** $\|P_{\text{prev-tok}} W_K\|_F / \|W_K\|_F$ at checkpoint $A$.

---

## 6. Pre-registered hypotheses

Thresholds fixed now, before data. Decision bands on $R$: **success** $R \ge 0.80$ · **failure** $R < 0.50$ · **ambiguous** in between, reported as ambiguous and never rounded into a verdict.

### H1 — Intrinsic dimension

*There is a rank $r^\ast$ at which the arm reaches criterion, and $R(r)$ tracks the singular-value spectrum of the fitted update.*

| Null | Statement | Falsified by |
|---|---|---|
| h0₁ | $R$ is flat in $r$ | Monotone rise in $R(r)$ |
| h0₂ | At $r < r^\ast$, 10× steps closes the gap | Plateau: $R$ stops improving while $< 0.50$ |
| h0₃ | Matched-$\beta$ unstructured update performs identically | Gap at matched $\beta$ |

h0₂ carries the scientific content: **reaches criterion slower → optimization limit; plateaus below → capacity limit.** No rank-level failure may be reported without both the matched-lr arm and the 10×-steps arm attached.

### H2 — Structural asymmetry

*$\varphi(\Delta M_{QK}) \approx 0.5$ and $\varphi(\Delta M_{OV})$ is materially below it.*

| Null | Statement | Falsified by |
|---|---|---|
| h0₄ | $\varphi_{QK}$ and $\varphi_{OV}$ are indistinguishable | Separation exceeding the matched-norm random-null band |

**Stated limitation, not to be omitted from any writeup:** random matrices also concentrate near $\varphi = 1/2$, so $\varphi_{QK} \approx 0.5$ against a random null is *uninformative*. The OV side carries the signal. Report the contrast, never $\varphi_{QK}$ alone as a positive finding.

### H3 — Separability

*$r^\ast_{QK} \ne r^\ast_{OV}$.*

| Null | Statement | Falsified by |
|---|---|---|
| h0₅ | The two sub-circuits share a minimum rank | Non-overlapping thresholds across seeds |

### H4 — Identity *(deferred scope, single instance)*

*The learned QK subspace aligns with the pretraining delta restricted to the same head.*

| Null | Statement | Falsified by |
|---|---|---|
| h0₆ | Principal angles indistinguishable from a random $r$-dim subspace | Mean $\cos$ above the 95th percentile of 100 random draws |

This is the only bridge to a developmental claim. Run it once, on the winning arm, on one matrix. Do not build the sweep.

---

## 7. What can and cannot be concluded

**Sufficiency is sound.** "Rank $r$ suffices to install prefix matching from $A$" is constructive — you exhibit the update. Upper bound on intrinsic dimension. A real result.

**Necessity is not obtainable.** Failure at rank $r'$ is failure of a tuple: this optimizer, objective, initialization, budget, parameterization, checkpoint. The problem is non-convex; there is no certificate that no rank-$r'$ solution exists. **"Rank $r'$ is insufficient" must never appear without its qualifiers.**

**The strongest available necessity evidence is M5, the truncation probe:** train at generous rank to get $\Delta W^\star$, truncate its SVD to rank $r'$, evaluate the truncation directly. This removes training dynamics from the inference entirely — it is evidence about the object rather than about the optimizer, and it is strictly better than a failed training run.

**Out of reach entirely:** "the minimum structure an induction head can take." That quantifies over all implementations in all models. Reachable claim: *the minimum-dimension update installing prefix matching from $A$, under this parameterization, has rank $\approx r^\ast$, and its QK component has antisymmetric fraction $\approx \varphi$.*

---

## 8. Gates and protocol

Ordered by kill-power per compute-hour. Each has unambiguous pass/fail. Do not proceed past a failure without a §10 entry.

**G0 — Transition location.** *(hours, forward passes only)* Sweep PMS per head and ICL over the checkpoint grid at 70m. Locate $A$ and $B$. Measure bracket width. **Fail:** no transition, or bracket wider than 3 consecutive checkpoints.

**G1 — Prerequisite.** *(minutes)* At $A$: does a previous-token head exist (prev-token score $\ge 0.3$)? Does $\|P_{\text{prev-tok}} W_K\|_F / \|W_K\|_F$ show non-negligible overlap on the candidate induction head? **Fail:** with $W_K$ frozen, no $\Delta W_Q$ at any rank can build the match — a null result here is a missing prerequisite, not a capacity limit, and reading it as the latter is the most expensive available mistake.

**G2 — CPU timing.** *(one run)* Wall-clock one generous-rank run to criterion at 70m. **This has never been measured; every schedule downstream assumes it.** Output decides CPU-only vs. GPU and the affordable sweep size.

**G3 — Positive control.** *(a day)* Generous rank on $W_Q$ alone. Does $R$ exceed 0.80 at all? **Fail:** the single-matrix composition rule (§3) is too restrictive; fall back to per-circuit adaptation and downgrade the H2 claim, or the objective/loop is broken.

**Main sweep, after all gates pass.** Per arm: rank $r \in \{1,2,4,8,16,32\}$ × lr (≥3 points, swept independently per rank) × 3 seeds. Parameterization triad at matched $\beta$. Plus M5–M8.

**Mandatory controls.** Matched-norm random rank-$r$ null. Matched-$\beta$ unstructured control. Held-out natural-text loss before/after. **Freeze LayerNorm and embeddings throughout** — otherwise $\Delta M_{QK}$ is not the only thing that changed and the symmetry analysis does not hold.

**The $\alpha/r$ confound.** Rank changes the effective learning rate on $\Delta W$. Either hold $\alpha/r$ fixed or sweep lr independently per rank. This is the single most likely way to produce a clean-looking curve that means nothing.

**Storage.** LoRA factors only, never merged models. Snapshot adapters every $N$ steps — they are tiny, keep all of them; they give the formation dynamics for free.

---

## 9. Falsification discipline

Method borrowed from the POPPER framework for automated hypothesis validation (Huang, Jin, Li et al. 2025, arXiv 2502.09858), with its own error analysis as the guide to what to avoid. Popper's underlying point is the operative one: **corroboration counts only in proportion to how severe the test was.** A hypothesis that survives a test which could easily have killed it earns more than one that survives a token test.

Taken from POPPER:
- Pre-registration of nulls and thresholds before data exists.
- Explicit implication chains: for each null, if the main hypothesis is false, is this null necessarily true? h0₃ exists as a relevance guard; h0₄'s stated limitation exists because the QK arm's implication chain is weak.
- Humans fix hypotheses and falsification tests. **Agents execute the protocol, produce plots, and flag threshold crossings.** POPPER's own error analysis puts the largest failure concentration in agent-designed tests.

Left behind deliberately: the e-value / sequential-testing aggregation. It controls Type-I error over a large unbounded space of generated tests against a static database. We have four hypotheses with one or two canonical tests each and we control data generation. The aggregation buys nothing and the α-ritual would manufacture false confidence.

POPPER's failure taxonomy, mapped:
- *Misinterpreted statistics (35.9%)* → reading "rank 2 got $R = 0.31$" as capacity when it is optimization. Guarded by h0₂ + M7.
- *Tests that break the implication (17.2%)* → guarded by h0₃ and by the h0₄ limitation note.
- *Ineffective test design (28.1%)* → guarded by G0–G3 running before compute is spent.

**Every status-board row that reaches ● must emit a results record containing:** the null tested, the pre-registered falsification criterion, the observed value, the verdict, and `METRIC_VERSION`. A row cannot be closed without it. This is schema-enforced in CI, not a convention.

---

## 10. Decisions log

Append-only. Never edit an entry — supersede it.

| Date | Decision | Reason |
|---|---|---|
| (init) | Target is capability-install, not delta-reproduction | Checkpoint grid too coarse; delta contaminated by general LM improvement (§1.1) |
| (init) | Success τ=0.80, failure τ=0.50, ambiguous between | Pre-registration; prevents post-hoc threshold fitting |
| 2026-08 | Headline model is pythia-70m, not 14m | Induction formation unreliable at 6L/4H/d=128; substrate risk not worth the speed |
| 2026-08 | LLC / SGLD dropped entirely | Target transition is known; detection machinery unnecessary |
| 2026-08 | Markov substrate, crosscoders, sparsity, agreement readouts dropped | Different question; superseded design |
| 2026-08 | Package renamed `sltdiff` → `indbw` | SLT is no longer in scope; the old name misdescribes the work |
| 2026-08 | Parity prediction ($r^\ast = 2$ from antisymmetry) **withdrawn** | Off-diagonal blocks have $\varphi = 1/2$ exactly at any rank; rank-1 achieves the maximum. Corrected prediction: rank set by $\operatorname{rank}(EP^\top)$, §3 |
| 2026-08 | Circuit split into prev-token / QK / OV with separate arms and metrics | Opposite predicted symmetry; a lumped metric conflates them |
| 2026-08 | Adapt one matrix per bilinear form | Cross terms destroy the rank and parity structure (§3) |
| 2026-08 | H4 (subspace identity) reduced to a single deferred instance | Full sweep is the largest time sink; one instance answers the first question anyone asks |
| 2026-08 | M5 truncation probe added | Best available necessity evidence; removes optimizer from the inference (§7) |
| 2026-08-08 | Fixed invalid YAML in `.github/workflows/ci.yml` (unquoted `run: echo "...: ..."` parsed as an illegal nested mapping) | Every push since the workflow was added had produced 0 jobs and an immediate failure — CI was never actually green despite the Setup-row-1 code existing since 57f2dc2. Caught while closing the "CI green on empty suite" row; wrapped the offending step in a block scalar (`run: \|`) |
| 2026-08-11 | `probes.py` takes tensors (attention patterns, OV/embedding/unembedding matrices, per-token NLL), never a `HookedTransformer` | Keeps probes unit-testable against hand-constructed toy inputs per CLAUDE.md's fixture rule, and decouples this row from `models.py` (still ○). Orchestration — running a model to extract these tensors — belongs to whatever calls the probes, not to probes.py itself |
| 2026-08-11 | `recovery()` returns the unclamped value; clamping for reporting is a separate `clamp_recovery()` call, not a flag | Matches §5 ("clamped for reporting, unclamped logged") as two composable functions instead of a boolean that silently changes return semantics |
| 2026-08-11 | Pinned `transformers==5.9.0` exactly in `pyproject.toml` | Unpinned installs pulled `transformers` 5.14.1, whose `GPTNeoXForCausalLM` renamed the LM head from `embed_out` to `lm_head`; `transformer_lens` 3.7.0's neox weight conversion hardcodes `embed_out` and crashes on load. 5.9.0 is `transformer_lens`'s own declared floor and confirmed to still have `embed_out` |
| 2026-08-11 | `models.py`'s checkpoint grid is read from `transformer_lens.loading_from_pretrained.get_checkpoint_labels`, not hand-maintained | Same rationale as the probes.py tensor-only decision above: avoids a second copy of the 154-step Pythia grid that could silently drift from what `HookedTransformer.from_pretrained` actually accepts |
| 2026-08-11 | `wrapper.sh` and `worker-bootstrap.sh` now run `pip install -e ".[dev]"` after checkout, before doing any work | The transformers pin fix above only affects a fresh install — `worker-bootstrap.sh` and `wrapper.sh` were only ever `source`-ing a venv baked into the AMI at image-build time, never reinstalling from `pyproject.toml`. A dependency fix merged to the repo would silently never reach an instance launched from an older AMI. Reinstalling on every launch makes the venv self-healing from git state; costs boot time, not correctness |
| 2026-08-12 | `lora.py`'s symmetric/antisymmetric parameterizations operate on the composed square delta directly (`rank // 2` vector-pairs, `U V^T ± V U^T`), not on `W_Q`/`W_O`'s native rectangular shape | Symmetry is undefined on a rectangular matrix (§3), so the constrained parameterizations can only apply to the square composed form. `rank` is forced even by the parity theorem (docs/mathematics.md §6), not a convenience choice — odd rank raises. This keeps lora.py's tested surface a pure array-in/array-out module, same decoupling pattern as algebra.py/probes.py; wiring a given parameterization's output into a real `HookedTransformer`'s `W_Q`/`W_O` under the "one matrix per form" rule (§3) is left to `train.py`, not yet implemented |
| 2026-08-12 | Added `src/indbw/gates.py` (not in CLAUDE.md's original module list) for G0's transition-location logic; `scripts/g0_sweep.py` orchestrates the real checkpoint sweep | Gate-detection (locate A/B on a checkpoint grid from a PMS/ICL series) is a one-time analysis of swept metrics, not a per-example metric itself, so it doesn't belong under probes.py's METRIC_VERSION hash. `locate_transition`'s operationalization of §2's prose ("baseline" = ICL at step 0; "transition" = first grid step where max-over-heads PMS crosses 0.1) is fixed here, before any checkpoint was swept, per §11's own requirement |
| 2026-08-12 | Fixed a real upstream bug blocking every checkpointed model load: `transformer_lens`'s pythia-checkpoint branch (`loading_from_pretrained.py`, still present at 3.7.1, the latest release) passes `token=os.environ.get("HF_TOKEN","")` unconditionally, unlike its sibling branches which guard empty tokens. `huggingface_hub`'s current httpx backend rejects the resulting `Authorization: Bearer ` header outright (`LocalProtocolError`), where the older requests-based backend sent it silently. Worked around with a monkeypatch in `models.py` (`get_token_to_send` treats `""` as `None`) plus a `transformer_lens>=3.7.1` floor pin | tier-3's own integration test (`test_load_checkpoint_step_zero_...`) was silently broken by this — it isn't on the push path (CLAUDE.md tier 3), so nothing caught the regression until G0 needed real checkpoint loads across the full grid. The fix is intentionally narrow (normalizes only the empty-token case) so it can't mask a real auth failure |
| 2026-08-12 | G0's sweep processes each layer's attention pattern via a forward hook (compute PMS increment, discard) rather than `model.run_with_cache` (retain all layers' patterns until the batch loop deletes the cache dict) | `run_with_cache` across pythia-70m's 6 layers was enough peak memory, on this box's ~1.8GB RAM baseline (already tight per §11's "torch+transformers imports alone use ~700MB"), to force sustained swap thrashing — one checkpoint at the pre-registered N_eval=512/T=128 didn't finish in 500s. The hook-based rewrite produces bit-identical PMS/ICL numbers (verified against the `run_with_cache` version on a tiny pilot) at ~2.4s/batch steady state (batch_size=4), ≈5.5 min/checkpoint, ≈14h for the full 154-step grid |
| 2026-08-12 | `CLAUDE.md`'s blanket "do not run AWS commands or launch instances" narrowed to explicitly permit worker-instance lifecycle (launch/terminate via the `wrapper.sh`/`worker-bootstrap.sh` pattern, self-stop via `teardown.sh`), scoped to this project's tagged resources; no IAM changes | Requested by the user to parallelize G0's ~14h single-box sweep across workers after a mid-sweep disconnect lost unsaved progress (only step 0 had been committed). The single-box sweep is memory-bound (§11, ~1.8GB RAM), not embarrassingly-parallelizable in-process, but is embarrassingly parallel across the 154 independent checkpoints given a fixed eval set — a good fit for the existing worker pattern |
| 2026-08-13 | Built out `schema.py` (previously a 9-line stub: just `METRIC_VERSION`) into a real `Criterion`/`ResultsRecord` schema, and used it to close G0 | G0's row couldn't legitimately reach ● without a schema-validated results record (CLAUDE.md "every row that reaches ● must emit a results record"), and no prior row had ever exercised that path. `Criterion` is a single `observed[metric] <op> threshold` comparison; a record's `criteria` is a tuple of these ANDed together, which handles compound gate criteria (G0: "fail if not found, OR bracket > 3") without inventing per-row verdict logic. `ResultsRecord.recomputed_verdict()` is what tier 2.5's new `tests/schema/test_verdict_recomputation.py` calls against every record in `results/` |
| 2026-08-13 | **G0 gate closed: PASS.** A = step 512, B = step 2000, bracket_width = 2 (≤ 3) | `scripts/close_g0.py` merged all 7 worker shards (154/154 checkpoints, no gaps) and ran `locate_transition`. Result committed to `results/g0_transition.jsonl`. The 7-way worker sweep itself had actually finished ~17.5h before this closing step ran — the workers pushed their shards and self-terminated per `infra/worker-bootstrap.sh`, but nothing then re-invoked `locate_transition` over the merged data or updated the status board, so the row sat at stale ⏳ with fully-collected data behind it. Flagged in §11 |
| 2026-08-13 | Operationalized $P_{\text{prev-tok}}$ (docs/mathematics.md §9) as the orthogonal projector onto the previous-token head's $W_O$ row space, built via `indbw.algebra.subspace_projector` (SVD-based numerical rank, not raw QR) | Neither PROJECT.md nor docs/mathematics.md gave $P_{\text{prev-tok}}$ a concrete construction — it's the residual-stream write-subspace of the head that writes $p_t$ (docs/mathematics.md §8), i.e. $\operatorname{col}(W_O^\top)$. Fixed here, before checkpoint A was inspected, matching `gates.py`'s existing G0 precedent for operationalizing prose definitions before seeing data |
| 2026-08-13 | G1's "non-negligible overlap" (PROJECT.md §8, no numeric cutoff given) operationalized as: observed ratio exceeds the 95th percentile of the same ratio over 100 random rank-matched subspaces, seed 0 | Reuses the random-subspace-null convention PROJECT.md §6 already pre-registers for H4's $h0_6$, rather than inventing a new bare threshold. Implemented in `indbw.gates.k_composition_overlap` |
| 2026-08-13 | G1's candidate induction head is identified from checkpoint B's argmax-PMS head (layer 3, head 6, from `data/g0_sweep*.jsonl`), not from A's own PMS | A is pre-transition by definition (§2: "no head above PMS 0.1"), so A's own argmax-PMS head is noise, not a real circuit. The head that matters for the frozen-$W_K$ prerequisite check is whichever one *becomes* the induction head — i.e. B's |
| 2026-08-13 | **G1 gate closed: PASS.** Previous-token head at layer 2/head 1 (score 0.356 ≥ 0.3); candidate induction head's (layer 3/head 6) $W_K$ overlap with that head's output subspace = 0.387 vs. null 95th percentile 0.360 | `scripts/run_g1.py`, single forward pass over checkpoint A (step 512) under the same fixed eval set as G0. Result in `results/g1_prerequisite.jsonl`. **Margin is narrow (0.027)** — flagged as a caveat in §11 rather than treated as a comfortable pass |
| 2026-08-14 | Built `src/indbw/train.py`: LoRA injection via forward hooks (not an in-place weight edit) on `HookedTransformer`, freezing every base-model parameter so B/A are the only trainable tensors; `unconstrained` parameterization only (matches `indbw.lora`'s shape convention exactly, so a learned (B,A) hands straight to it for later symmetry/rank analysis) | G2 needed a real training loop to time; §3's "adapt exactly one matrix" and §8's "freeze LayerNorm and embeddings" needed to be mechanically enforced (no trainable path to anything but B/A) rather than left as a convention a future row could violate by accident |
| 2026-08-14 | §11's open question ("Induction objective... unresolved and consequential — settle before G3") settled: **synthetic repeated-random second-copy NLL**, minimized directly. This is the "cleanest" option §11 itself named | G2 already needs an objective to time a run against, and G2 precedes G3 — deferring further wasn't available. The named risk (a circuit that only works on synthetic input) is exactly M8's job to catch, not this decision's to prevent |
| 2026-08-14 | `train.py`'s `second_copy_nll`/`first_copy_nll` windowing matched exactly to `scripts/g0_sweep.py`'s existing (already-committed, G0/G1-closing) convention — both exclude the copy-boundary position (index T-1, predicting token T) from either half, rather than including it in the second copy as a naive reading of §5 might suggest | G2/G3 reuse G0's already-recorded ICL(A)/ICL(B) as recovery baselines (PROJECT.md §5's `recovery()`) rather than recomputing them from scratch — a caught-before-running mismatch: my first draft of `second_copy_nll` included the seam position, which would have silently computed `R` against baselines measured on a different windowing than the one being evaluated. Fixed before any real checkpoint was touched, same discipline as G0/G1's own pre-registered operationalizations |
| 2026-08-14 | G2's CPU-vs-GPU decision rule operationalized *before* this run's timing is known (PROJECT.md §8 names no numeric threshold): project the 108-cell main sweep's wall-clock using a 7-way worker fleet (G0's precedent fleet size); ≤24h → CPU stays affordable, else escalate to GPU. Recorded as a planning estimate alongside G2's results record, not folded into its pass/fail verdict | G2's stated purpose is exactly this decision ("Decides CPU vs GPU") but PROJECT.md gives no number to compare against — same category of gap as G0/G1's ambiguous prose, closed the same way: fix the rule before the data that would tempt post-hoc fitting exists |
| 2026-08-14 | Built `src/indbw/sweep.py` + `scripts/run_sweep.py` for M1/M2, implementing three CLAUDE.md requirements that existed nowhere in the repo: **config as data** (`CellConfig` frozen dataclass, `run_id` = sha256 of its canonical JSON, and that hash *is* the record's `run_config_hash`), **resumability** (`completed_run_ids`/`pending_cells` recompute the pending set from what is on disk; a stopped instance costs one cell), and **the mandatory-control gate** (`check_failure_claims`, now run in tier 2.5 over every record) | `close_g0.py`, `run_g1.py` and `run_g2.py` each inlined their own ad-hoc config dict and sha256 call, so "the hash is the run ID" was three separate conventions rather than one object, and nothing could resume. The control gate matters most: CLAUDE.md's "No failure claim without its controls attached" was a rule someone had to remember at writeup time, and is now a computation that fails the build. §6's decision bands (`decision_band`) live in sweep.py rather than probes.py — they are pre-registered *decision thresholds* applied to a metric, not a definition of one, so they stay out of the METRIC_VERSION surface |
| 2026-08-14 | The main sweep holds α/r fixed **and** sweeps lr independently per rank, rather than picking one | §8 offers them as alternatives ("Either hold α/r fixed or sweep lr independently per rank... the single most likely way to produce a clean-looking curve that means nothing"), but each leaves a different residual: a fixed α/r still leaves the optimizer's own rank sensitivity, and a per-rank lr sweep still leaves the parameterization's scale drifting with r. Doing both costs nothing beyond writing it down. `enumerate_cells` requires an independent lr list per rank and raises if one is missing or has < 3 points, so the confound cannot be reintroduced by accident |
| 2026-08-14 | The per-rank lr ladder is a geometric 3-point grid **centred on the lr G2's generous-rank run actually reached criterion with**, read from G2's artifact at sweep-launch time | §8 requires ">= 3 points" per rank but names no values, and inventing three numbers before any training run existed would have been a guess dressed as a protocol. Anchoring on a measured working point defers the number to data the project already paid for. `run_sweep.py` refuses to start if G2's artifact is absent rather than falling back to a default |
| 2026-08-14 | `run_sweep.py` refuses to start unless G0–G3 all have a passing *and* self-consistent record | §8: "Main sweep, after all gates pass"; CLAUDE.md: "A gate that fails stops the phase... Do not route around it." That ordering was previously enforced only by whoever launched the instance. The self-consistency check is the same one tier 2.5 makes — a record whose stored verdict contradicts its own numbers cannot license anything downstream even if it says "pass" |
| 2026-08-14 | **CI has been red since 5d88030** (three `mypy --strict` errors in `train.py`: `fwd_hooks` typed `list[tuple[str, object]]` against transformer_lens's declared `list[tuple[str \| Callable, Callable]]`, twice, plus an untyped `loss.backward()`). Fixed by adding a `HookList` alias matching TL's own annotation and one targeted `no-untyped-call` ignore | Tier 0 fails → every downstream tier is skipped via `needs:`, so *no* test has run on CI since that commit, and per CLAUDE.md's CD section (`wrapper.sh` step 4, "proceed only if green") the work loop was gated shut the whole time. §11's note that mypy cannot be run locally is exactly why this survived: the only place the gate runs is CI, and nothing was reading CI. No behavioral change — annotations and a comment only |
| 2026-08-14 | Tier-0 metric-hash mechanism designed and wired in (`src/indbw/metric_hash.py`, baseline `metric_hash.json`, CLI `scripts/check_metric_hash.py`, plus a pre-commit hook). Hashes every top-level definition in `algebra.py` and `probes.py` via docstring-stripped `ast.dump` — insensitive to formatting/comments/docstrings, sensitive to any executable change; an unrecognized top-level construct raises rather than being skipped | `ci.yml` had carried this as an `echo` stub since the workflow was written ("not yet designed... design it test-first in a work session"), so CLAUDE.md's central protection against silently invalidating prior results did not exist. AST-normalizing rather than hashing file bytes is the load-bearing choice: a gate that forces a METRIC_VERSION bump for a typo fix is a gate people route around. `update()` refuses to regenerate the baseline unless `METRIC_VERSION` was bumped first — that refusal, not the docs, is what makes the rule mechanical |
| 2026-08-14 | Tier-2 smoke test added (`tests/smoke/test_smoke.py`, run by CI tier 2 alongside `tests/property`) | CLAUDE.md's TDD contract item 5 specifies it exactly ("load toy model → inject LoRA → 5 training steps → compute every probe → emit a results record → validate schema... under 60 seconds") and it had never been written. It is the only test that exercises `train.py`'s hook injection and `probes.py`'s TL weight-layout assumptions together — the REVIEW.md hook-ordering risk and any TL version bump land here first. Runs in ~4s and asserts nothing about science |
| 2026-08-14 | h0_3's "matched-β unstructured control" operationalized as a random **coordinate** subspace (`nulls.matched_beta_support`: exactly β = r(d_in+d_out) free entries, support fixed before training, never resampled), not the dense random projection of Li et al. 2018 | Settles §11's "fixed or resampled" open question in the direction §11 itself leans (fixed is the LoRA analogue). The coordinate-vs-dense part is forced: at the QK arm's shapes a dense P would be 32768 × 36864 floats (~9.7 GB) at the generous rank, and β would exceed the ambient dimension, making the "control" an unconstrained full matrix. `matched_beta_support` raises in that regime rather than silently becoming vacuous. Flagged in REVIEW.md for sign-off |
| 2026-08-14 | `bandwidth` / `bandwidth_fraction` (PROJECT.md §5's β) live in `lora.py`, not `probes.py` | β is arithmetic on a config — computable before a forward pass — and putting it in `probes.py` would enlarge the METRIC_VERSION surface with something that never reads a model, forcing a version bump (and thereby staling every historical record) for a change that cannot affect any measurement |
| 2026-08-14 | `scripts/run_g3.py` written before G2 finished, deriving G3's record from G2's artifact + G2's own results record and cross-checking the two | Follows the already-logged G2/G3 compute-sharing decision below. The cross-checks are the point: it refuses on a `budget_exceeded` artifact, on a record/artifact disagreement, on a run-hash mismatch, and — most importantly — on a non-finite recovery, since `nan >= 0.80` is False and would silently record "the positive control failed", stopping the phase for a reason that is not the science |
| 2026-08-14 | `scripts/run_g2.py`'s real training run (generous rank r=64=d_head on $W_Q$ at the layer-3/head-6 induction head, checkpoint A) is written to also produce a raw JSON artifact (`data/g2_generous_rank_run.json`) that `scripts/run_g3.py` reads to close G3 without retraining | G2's timing run and G3's positive control ask for the literal same experiment (generous-rank $W_Q$-only adaptation from A, does R reach 0.80) under two different framings (how long vs. does it happen at all) — retraining a second time to answer a question the first run already answered burns exactly the compute-hours CLAUDE.md's provenance/resumability sections exist to conserve. Each row still gets its own commit, board update, and schema-validated results record |
| 2026-08-14 | Diagnosed and fixed a box-specific memory-exhaustion bug that had corrupted the prior session's G2 measurement: this instance has 1.8GB RAM and booted with **no swap active** (a `/swapfile_g2` existed on disk from a prior session but was never `swapon`'d after reboot). The first G2 attempt this session was silently OOM-killed by the kernel (confirmed via `journalctl -k`, repeated `oom-kill` events on `python3`, not the graceful `TrainingBudgetExceeded` path); a direct profiling script (isolated forward+backward, no training loop) confirmed real per-step compute is fast (~3.7s: load 7.7s, fwd 2.0-2.2s, bwd 1.7s) once swap was reactivated — the earlier 2700s "budget-exceeded" result in the prior session's uncommitted `results/g2_cpu_timing.jsonl` was discarded (never committed) as an artifact of this, not a genuine timing measurement | Distinguishing "CPU is slow" from "this box is out of memory" matters directly for G2's stated purpose (§8, "decides CPU-only vs. GPU") — attributing memory-exhaustion stalls to CPU compute cost would have been exactly the kind of misattributed-cause error CLAUDE.md's rigor sections exist to prevent, just applied to an infra decision instead of a scientific one |
| 2026-08-14 | Found and fixed a second, independent memory bug: `indbw.train.compute_recovery`'s default `batch_size=32` materializes a full-vocab logits tensor of `batch_size * 2T * d_vocab * 4` bytes per batch — at `T=128`, `d_vocab=50304` that's ~1.65GB for one batch, alone exceeding this box's 1.8GB RAM. This fired twice: once during G2's *periodic* training-time eval (mitigated by shrinking `run_g2.py`'s `TRAIN_EVAL_N` from 128→16, since `compute_recovery`'s internal batching only forms one sub-batch when `eval_n < batch_size`), and again during the canonical **final** N_eval=512 eval after training had already cleanly finished all 400 steps (the run had to be killed after the eval hung in swap-thrashed D-state for >15 min; the *training* result was recovered from the already-written `data/g2_snapshots/step_000400.npz` periodic snapshot via a one-off `scripts/_resume_g2_from_snapshot.py`, rather than repeating the 2198s of already-completed training). Fixed at both call sites (`train.py`'s periodic eval now passes `batch_size=8`; `run_g2.py`'s final eval now passes `batch_size=4`) so future runs on this box don't repeat either failure mode | CLAUDE.md's resumability principle ("losing twenty hours of CPU to a stop is a self-inflicted wound") applies at the single-run scale too — discarding 2198s of real, already-converged (if unsuccessful) training compute over a fixable eval-batching bug would have been wasteful. Also added a per-eval-checkpoint progress print inside `train_lora` (step, loss, R, elapsed) — its absence is why the earlier failures took this long to diagnose: a training run with no output looks identical whether it's progressing normally or already stalled |
| 2026-08-14 | **G2 gate closed: CPU affordable.** 400/400 steps (max_steps reached, criterion not reached) in 2198s (5.5s/step) for the generous-rank (r=64) QK-only run from A. Projected full 108-cell main sweep on a 7-way worker fleet: 9.8h, under the pre-registered 24h threshold (§10, 2026-08-14 "CPU-vs-GPU decision rule" entry) → **CPU stays affordable** for the main sweep, assuming other worker instances are provisioned with adequate RAM (unlike this interactive sandbox, which needed the swap + eval-batching fixes above just to get one clean run) | This is the number G2 exists to produce (§8: "This has never been measured; every schedule downstream assumes it"). The caveat about worker RAM provisioning is carried into §11 rather than silently assumed away, since this session's own experience shows what happens when it isn't met |
| 2026-08-14 | **G3 gate failed** (reused G2's run per the shared-compute design above, no retraining): final canonical $R = 0.0117$, far below the 0.80 criterion. **Decision taken:** treat this as an *inconclusive* gate failure, not a validated finding about the single-matrix composition rule. The training curve shows essentially no learning signal at all — loss stayed in a narrow 12.8–13.0 band for all 400 steps with no visible downward trend, and $R$ crept from 0.004 to 0.014 in an almost-flat line — which looks much more like an optimization/tuning failure (wrong lr, or a subtler bug in how the QK delta reaches the loss) than a genuine "rank/structure insufficient" result, which would typically show real early progress before plateauing. Per CLAUDE.md ("a gate that fails stops the phase... do not route around it"), M1–M4 are **not** started this session; a diagnostic follow-up (e.g., an lr sweep or a hand-verified toy-model QK-only convergence check, `tests/unit/test_train.py`'s own OV-not-QK note being the closest existing precedent) is needed before either accepting this as informative about §3's composition rule or re-running G3 under corrected settings. Flagged in REVIEW.md for human judgment on which branch applies | §7's necessity caveat ("failure at rank r' is failure of a tuple: optimizer, objective, initialization, budget...") applies here even though G3 isn't itself part of the M-phase rank sweep — declaring a scientific verdict about the composition rule from a run whose own loss curve shows no learning at all would be exactly the "misinterpreted statistics" failure mode POPPER's taxonomy (§9) warns about |
| 2026-08-14 | Ran the lr diagnostic named above: 6 workers (t4g.small, ~2 min compute each), one lr per worker (1e-4, 1e-3, 3e-3, 1e-2, 3e-2, 1e-1), 100 steps each, same rank=64/QK-arm/checkpoint-A/objective as G2/G3 (`scripts/diagnose_g3_lr.py`, `data/g3_lr_diag_worker{0-5}.jsonl`). **Result: this updates, and partly overturns, the "optimization/tuning failure" reading above.** Every lr shows a real, systematic, lr-dependent gradient signal — $R$ rises monotonically at low-to-moderate lr (1e-4 through 1e-2) and shows classic too-high-lr flattening/noise at 3e-2 and 1e-1 — which rules out "the objective/loop is broken" as the likely explanation (a broken gradient path would not produce this coherent an lr-response curve). But **no lr gets anywhere close to 0.80**: the best (lr=1e-2) reaches only $R=0.0095$ by step 100, and the increments shrink step over step (a saturating curve, not a linear one). This matches G2/G3's own 400-step trajectory at lr=3e-3 almost exactly in shape: $R$ rises to a peak of $\approx 0.0144$ around step 320, then *wobbles back down* to 0.0129 by step 400 — i.e. 300 additional steps past the 100-step mark bought essentially nothing. **Two runs, six lr values, and a 4x step-count comparison all show the same saturating plateau around $R \approx 0.01$–$0.015$, two orders of magnitude below criterion.** This is now much more consistent with a genuine capacity/structural limit than with a tuning problem, though it does not by itself prove which structural limit (see the new §11 entry on the $W_K$-bottleneck hypothesis this suggests). No further hyperparameter search was run past this point — the pattern across lr and step count is consistent enough that more lr values are unlikely to change the qualitative picture, and the more informative next experiment is a different one (see §11), not a bigger grid |
| 2026-08-15 | Ran the $W_K$-bottleneck diagnostic that §11's 2026-08-14 UPDATE entry named and flagged as needing human sign-off (REVIEW.md, same date) — **user explicitly authorized this specific diagnostic** before it ran, since CLAUDE.md reserves new hypothesis tests for humans. `src/indbw/train.py` gained `qk_both_hooks` (diagnostic-only, never reachable from `build_hooks`/`TrainConfig`/`Arm` — §3's "one matrix per form" rule is untouched for G2/G3/M1/M2), covered by 5 new unit tests mirroring `qk_hooks`' existing zero-init/discrimination/causality/gradient guards (`tests/unit/test_train.py`, tier 1+2 green: 370 passed, only the pre-existing documented 3.11-vs-3.12 metric-hash false positive from §11 unrelated to this change). `scripts/diagnose_g3_wk_bottleneck.py`: same checkpoint (A, step 512)/layer/head (3/6)/objective/lr (1e-2, the lr-diag's best) as before, but now rank=64 on **both** $W_Q$ and $W_K$ (generous on each), 150 steps. **Result: $R$ plateaued at 0.0051 — not better than QK-only G3's 0.0117, and the loss curve (12.8–13.1, noisy, no downward trend across 150 steps) is indistinguishable in shape from every prior QK-only attempt.** This disfavors the $W_K$-bottleneck hypothesis: unfreezing the one matrix that hypothesis says is the ceiling produced no improvement at all, when a real ceiling being lifted should have shown at least some early upward movement even if not reaching criterion. Logged as a diagnostic result (`data/g3_wk_bottleneck_diag.jsonl`), not a schema-validated PROJECT.md record — same non-gate status as the lr diagnostic. **Does not reopen or re-pass G3**; the gate stays failed and the phase stays stopped per CLAUDE.md. A new candidate explanation this raises — untested, flagged in §11 below rather than pursued further this session — is that the fixed candidate head (layer 3/head 6, chosen from $B$'s argmax-PMS per the 2026-08-13 decision above) may not be the head gradient descent would naturally recruit for induction when starting from $A$, where no head has any PMS structure yet |
| 2026-08-15 | Ran the wrong-target-head diagnostic — **user directed this specific follow-up.** Before training anything, checked the existing G0 sweep data (`data/g0_sweep*.jsonl`): at every checkpoint before $A$ the argmax-PMS head bounces between different heads at pure-noise scores ($[5,7]$ at steps 0–16, $[3,0]$ at 32–64, $[4,0]$ at 128, $[0,3]$ at 256), and only at $A$ itself (step 512) does argmax land on $[3,6]$ — which is then the head whose PMS explodes to $>0.9$ by step 1000. Mild evidence against "wrong head" for natural full-pretraining dynamics, logged as context, not a substitute for a direct test. Ran `scripts/diagnose_g3_head_choice.py` (reuses `train.py`'s existing QK arm unchanged — no new hook mechanism, no new tests needed) on three alternative candidates at lr=1e-2/rank=64/100 steps, matching the lr-diagnosis's best cell exactly except for which head: $(2,1)$ the prev-token head itself, $(3,0)$ same layer as the trained head, $(4,6)$ one layer deeper. One cell $(4,6)$ was run both locally and (redundantly, by a coordination slip — see §13) on a launched EC2 worker; the EC2 worker was terminated once the local result landed, before it could push, so no duplicate record exists. **Result: all three alternatives plateau lower than layer 3/head 6's own $R=0.0095$ at the identical lr/step budget** — $(2,1)$: 0.0035, $(3,0)$: 0.0016, $(4,6)$: 0.0030 (`data/g3_head_diag_worker{0,1,2}.jsonl`). Layer 3/head 6 is not merely adequate among these four — it is the best of them. **This disfavors the wrong-target-head hypothesis.** Four candidate explanations for G3's plateau are now ruled out or disfavored (broken objective, insufficient rank, frozen-$W_K$ ceiling, wrong head) and none has been confirmed as the true cause |
| 2026-08-15 | Ran the full fine-tune sanity-ceiling diagnostic — **user-approved next step**, discussed after four negative diagnostics in a row left two live explanations undistinguished: (a) a genuine capacity/structural limit under the single-matrix composition rule (§8's named fallback branch), or (b) something shared across every run so far — the synthetic objective's difficulty from checkpoint $A$, or the step budget — that has nothing to do with which matrix is frozen. `scripts/diagnose_g3_full_finetune_ceiling.py` unfreezes **every** parameter (including LayerNorm and embeddings — explicitly not the M1–M8 protocol's rule, deliberately violated only because this is a diagnostic), full-fine-tunes from $A$ on the identical synthetic objective, two lr values (1e-4, 1e-5), planned 80 steps. **Result: neither run got anywhere near G3's own floor, let alone criterion.** Both hit the 1800s per-cell wall-clock budget before 80 steps (worker0/lr=1e-4: 38/80 steps, final $R=0.0022$; worker1/lr=1e-5: 53/80 steps, final $R=0.0047$) — *lower* than QK-only G3's $R=0.0117$ and the best head-choice cell's $R=0.0095$, despite training loss falling substantially in both runs (worker0: 13.27→11.13; worker1: 13.27→11.18), converging toward but not below the ≈10.83 chance-level NLL ($\log 50304$). **Notably, $R$ itself declined monotonically over training in both runs** (worker1: 0.00686 at step 10 → 0.00472 at step 50; worker0: 0.00253 at step 10 → 0.00225 at step 30) even as train loss fell — the generic loss reduction is not translating into second-copy-over-first-copy predictive advantage on the fixed eval set, and if anything is moving the wrong way. `data/g3_full_ft_worker{0,1}.jsonl`. **This disfavors explanation (a):** removing every constraint the composition rule imposes — training the whole model, not just one matrix — still could not move $R$ off the floor, and did no better than the constrained LoRA runs. It is more consistent with **explanation (b)**: something shared across every diagnostic so far (the objective, the checkpoint, or the step budgets — all far short of what real pretraining needed to grow the same circuit) rather than which matrix is frozen. **Caveats, flagged in REVIEW.md rather than resolved here:** both runs were themselves budget-truncated before their planned 80 steps with loss still trending down when cut off; only two lr values were tried, chosen by convention (not swept); and the declining-$R$-while-loss-falls pattern is itself unexplained and could reflect eval-set noise at these tiny recovery magnitudes rather than a real regression. Suggestive, not conclusive | Five negative diagnostics now share the same unexplained floor (broken objective/loop, insufficient rank, frozen-$W_K$, wrong head, and now no-composition-constraint-at-all); §11 updated with the human-facing question this raises |

---
| 2026-09-01 | The instance venv is pinned to Python **3.11** and provisioned by the shared fleet (`infra/rvm.env`, `RVM_PYTHON=3.11`), not baked into the AMI | §11 recorded the 3.11-vs-3.12 gap as an unfixable local limitation ("no 3.11 interpreter is installed locally"), forcing both `mypy src` and the tier-0 metric-hash test to be verified via CI rather than pre-flight. `uv` installs 3.11 at boot, so the instance now runs what CI runs: `pytest tests/unit tests/property` goes from 434 passed / 1 failed under 3.12 to **461 passed / 0 failed** under 3.11, with `test_committed_lockfile_matches_current_metric_source` green. The venv is also 5.2 GB → 1.5 GB, because a default `pip install` resolves torch to a CUDA build even on aarch64 — 2.9 GB of `nvidia/*` that cannot execute on a GPU-less t4g |
| 2026-09-03 | **7th G3 diagnostic: reachability graft.** Grafted checkpoint $B$'s real weights into $A$ component-by-component (`scripts/diagnose_g3_reachability.py`, `data/g3_reachability_diag.jsonl`, worker `g3reach`, on-demand `t4g.medium` — spot was down for this account at launch time, see REVIEW.md) to test whether the QK arm's criterion is reachable *at all*, independent of optimization. Controls hold: `full` = 1.0004, `base_a` = −0.0002, plus two unasserted consistency checks pass (`ov` leaves PMS bit-identical to `base_a` since OV cannot move attention; `qk_qk` moves PMS further than `qk_q` alone). **`qk_q` — B's real $W_Q$ for head (3,6), the exact tensor the QK arm trains — gives $R = -0.0006$.** This upper-bounds what any $\Delta W_Q$ at any rank, lr, or step budget can reach from $A$: it moves nothing, so G3's plateau is not an optimization failure in the QK arm. The result overshoots the question, though: the whole head gives 0.0046 and the whole host block (layer 3, all matrices + MLP + LN) gives 0.0077 — the same ~0.01 floor as every prior optimization diagnostic — while `full` gives 1.0004. **The gap between `layer_host` and `full` is real, large, and entirely unlocalized** — nothing between those two cells was measured. Also unresolved: `full`/`base_b` read 1.0004, not exactly 1 (measured ICL 11.5170 vs. the reused `ICL_B = 11.5120` constant, a ~0.04% drift never previously checked against a re-measurement on this hardware; if real, every prior $R$ carries the same small bias). **No verdict recorded** — whether the gap means the circuit is distributed outside block 3, $A$ and $B$ are not in the same loss-landscape basin so grafts don't compose, or LayerNorm/embedding co-adaptation, is a human call per CLAUDE.md §9, and the three have different consequences for whether M1–M8 is well-posed. Full detail in REVIEW.md 2026-09-03 | This is the discriminating result the 2026-08-14/08-15 diagnostics (lr, rank, frozen-$W_K$, head choice, full fine-tune) were converging toward: for the first time, a constructive (no-training) witness rather than another failed optimization run |
| 2026-09-05 | **Localization bisection run (the six new graft cells from commit 96a1100, PROJECT.md §11 2026-09-03's "Candidate next diagnostic").** First attempt as one 10-cell job on a single spot `t4g.medium` lost the entire run to a mid-run reclaim (`Server.SpotInstanceTermination`, ~45 min in) with nothing pushed — `diagnose_g3_reachability.py` only syncs/pushes once, after every requested cell finishes, so a reclaim costs the whole job, not "one cell" as CLAUDE.md's incremental-write principle intends (flagged in REVIEW.md). Re-run as 10 independent one-cell spot workers instead (`g3local-*`, each its own `--out` shard, merged into `data/g3_reachability_diag.jsonl` afterward): 9/10 landed on their first or second attempt. `layer_host_plus_ln_final` did not land after 6 consecutive spot attempts on `t4g.medium` (5 confirmed `Server.SpotInstanceTermination` reclaims; the 6th is unconfirmed — its instance aged out of the EC2 API before it could be checked, and neither git nor the S3 backstop shows a result) — dropped for now at the user's direction rather than continuing to retry or switching to on-demand. Every worker logged peak RSS via the now-standard `/usr/bin/time -v` instrumentation: **~2.69 GB across all 9, regardless of cell** — confirming the memory cost is loading $A$ and $B$ simultaneously, not which cell is grafted, and that `t4g.medium` (4 GB) is sized correctly (not oversized) for this script. `base_a`, `layer_host`, `full`, `base_b` reproduce the original single-worker run bit-for-bit (`diff_R = 0.00e+00`) on different underlying hardware — the diagnostic is deterministic. **Result: PMS-restoration and R-restoration dissociate cleanly, for the first time measured rather than argued from §4.** Grafting the layer-2 prev-token head alone (`layer_host_plus_prevtok_head`, one head's $W_Q,W_K,W_V,b$) lifts PMS $0.0212 \to 0.3682$ — crossing §5's own PMS$\,\ge 0.3$ induction-bearing bar from a single head. All of block 2 lifts PMS to $0.7260$. Blocks 0-2 together with block 3 (`layer_host_plus_pre`) lift PMS to $0.8949$ — essentially `full`'s $0.9641$ — while **R reaches only $0.1010$**, even though this cell already contains $B$'s real $W_V, W_O$ for head (3,6) (`layer_host` always grafts the whole block). Blocks 4-5 alone move nothing ($R=0.0096$, PMS$=0.0212$, identical to `layer_host`'s own floor). `layer_host_plus_embed_unembed` raises $R$ to $0.0212$ without moving PMS at all ($0.0223$), and its `nll_first` ($17.14$) is *higher* than `base_a`'s own $13.11$ — $B$'s unembedding read through $A$'s residual stream is actively worse than $A$'s untouched output, not a generic improvement. **No verdict recorded.** Whether the missing $\sim 90$ points of $R$ live in `ln_final` alone (unmeasured), in combinations this bisection didn't test (each cell adds to `layer_host` independently, not cumulatively past `pre`), or reflect $A$/$B$ not composing as a basin-mismatch problem that no single-component graft can fix, is the open question — §11 | Separates, for the first time, "prefix matching is reachable by grafting" from "recovery is reachable by grafting" — previously one number (`layer_host`'s $R=0.0077$) stood in for both, and §4's warning that a lumped metric hides which component is the bottleneck was argued, not shown. Also resolves the "which head" side of the multi-head question for this diagnostic specifically: `layer_host` grafts all of block 3's heads in every cell, so heads (3,1)/(3,0) alongside (3,6) are already present throughout — the missing structure is not "the wrong head" |
| 2026-09-03 | **G1's prerequisite, measured at $B$ for the first time.** G1 (§8) only ever measured the previous-token-head prerequisite at $A$; `scripts/diagnose_prereq_ab.py` (`data/prereq_ab_diag.jsonl`, `t4g.small` spot worker, peak RSS 1.34 GB) measures it at both. $A$'s re-measured numbers reproduce the committed G1 record to 3 decimals (0.356 / 0.387 / 0.360) — validation that the eval set and hook wiring agree with the original gate. **The contrast:** prev-token score $0.3556 \to 0.9403$ (2.6×); $W_K$-overlap ratio $0.3871 \to 0.7691$ (2.0×); overlap margin over the random-subspace null $0.0268 \to 0.4081$ (15×) — same head (layer 2, head 1) at both checkpoints, so this is one component strengthening, not a different one appearing. G1 passed at $A$ on both criteria by margins its own §11 note already flagged as narrow (0.356 vs. a 0.3 bar; a 0.027 margin). At $B$ neither statistic is close to its threshold. **Unplanned second observation: three layer-3 heads show prefix matching at $B$** — (3,6) 0.9652, (3,1) 0.8993, (3,0) 0.5094 — against uniform ~0.006 at $A$. §4's protocol adapts a single head. **No verdict recorded on either point.** This is a two-checkpoint correlation from natural pretraining, not a causal or developmental claim (reserved for D1, which has not run); whether it means M1–M8 needs rescoping to account for a possibly multi-head target, whether G1's 0.3 bar was ever derived from this model rather than convention, or whether the phase is still well-posed as specified are all live readings a human must choose among. Full detail in REVIEW.md 2026-09-03 | Supplies a candidate single mechanism for results that had been accumulating as separate negatives: the deficit sits at layer 2, upstream of layer 3, which every G3 diagnostic varied something in or around — consistent with, though not proof of, why grafting all of block 3 still left $R$ at 0.0077 |
| 2026-09-05 | **User sign-off on three standing REVIEW.md readings, closing them before M3/M4/M6 can inherit the ambiguity.** (1) `check_failure_claims`'s "matched-lr arm" (CLAUDE.md, h0₂) means the full per-rank lr sweep: a rank counts as a capacity failure only if all ≥3 swept lr points at that rank fail, not a single run at one reference lr — confirmed as the stronger, intended control (REVIEW.md 2026-08-14, row 1). (2) h0₃'s matched-β control stays a fixed random **coordinate** subspace (`nulls.matched_beta_support`), not a dense random projection, despite the axis-alignment caveat that a coordinate-aligned Δ*W* could be favored — confirmed as the intended control (REVIEW.md 2026-08-14, row 2). (3) h0₄'s separation band stays `nulls.phi_separation_null_band`'s reading: the 95th percentile of $\|\varphi_{QK}-\varphi_{OV}\|$ over *paired* independent matched-norm null draws, not two separate one-arm bands — confirmed (REVIEW.md 2026-08-14, row 3). No code changed for any of the three; each was already implemented under the now-confirmed reading. None of this touches the G3/M1 block — M1–M4 remain blocked on the localization question below | Three definitional readings that materially affect what a future h0₂/h0₃/h0₄ failure or pass would mean were sitting as open questions since 2026-08-14; settling them now means M3/M4/M6 can run against a confirmed protocol the day the phase unblocks, instead of re-litigating the reading under time pressure then |
| 2026-09-05 | **`layer_host_plus_ln_final` dropped for good — 8/8 spot launches reclaimed across two instance sizes (`t4g.medium` ×7, `t4g.large` ×1), all confirmed `StateReason.Code = Server.SpotInstanceTermination`; every other localization cell succeeded within 1-2 attempts through the identical launch path. User decision: stop retrying rather than authorize on-demand or pin an AZ.** The localization bisection's answer therefore stays bounded by `layer_host_plus_pre` (blocks 0-3 grafted: PMS $0.8949$, $R = 0.1010$) — whether LayerNorm-final specifically would close some of the remaining gap to `full` ($R=1.0004$) is now permanently unmeasured under this diagnostic, not merely unmeasured-so-far. Ruling out instance size (row above) leaves no untried explanation for the 8/8 pattern that this project has permission to investigate — `ec2:DescribeSpotInstanceRequests` is denied to this role, so whether it was AZ-specific capacity contention, something about this exact `WorkerId`/cell combination, or coincidence is unresolved and, per this decision, will stay that way | Eight identical-shape launches failing the same way while ten-plus others succeeded is itself a finding worth keeping, separate from the scientific question: any future attempt to re-run this exact cell should expect the same result and needs a different launch strategy (on-demand, explicit AZ, or a different account/region) to have a real chance, not just another spot retry |
| 2026-09-05 | **Four REVIEW.md engineering items actioned, none touching a metric definition or a hypothesis.** (1) Added a unit test pinning TransformerLens's hook-firing order that `qk_hooks`/`qk_both_hooks`/`ov_hooks` depend on (REVIEW.md 2026-08-14, row 4) — and found, in writing it, that `ln1.hook_normalized` actually fires **three times** per block per forward pass (once per Q/K/V projection), not once as every prior description assumed; all three firings are bit-identical, so the existing capture-then-overwrite pattern is correct by accident of that identity, not because it only fires once. Both facts are now asserted directly (`tests/unit/test_train.py`, 5 new tests) rather than being an unstated premise. (2) `metric_hash.json` now records the Python version that generated it (`"3.11"`, matching CI and the §11/REVIEW.md 2026-09-01 diagnosis); `check()` now reports a version-skew warning instead of "bump METRIC_VERSION and regenerate" when the lockfile's recorded version disagrees with the running interpreter's (REVIEW.md 2026-09-01, row 25) — directly closes the trap that entry flagged, where following the gate's own failure message on a 3.12 box would have baked a wrong baseline in. Old lockfiles without the field read as `"unknown"` and still get real-change detection, not a silent exemption. (3) `diagnose_g3_reachability.py` now syncs each cell's output to S3 immediately after it's written (`sync_to_s3`, same best-effort non-raising contract as `g0_sweep.py`'s), rather than only after every requested cell in the run finishes (REVIEW.md 2026-09-05, row 33) — the exact gap that turned one spot reclaim into a lost 45-minute job two days ago. (4) Did **not** delete `infra/rvm.env`'s `RVM_INSTANCE_TYPE` as the REVIEW.md row suggested — that note characterized it as dead because neither `infra/wrapper.sh` nor `infra/worker-bootstrap.sh` reads it, which is true, but `rvm.env`'s own header says it is read by the external `research-vm-infra` fleet system at orchestrator boot, a consumer outside this repo that nothing here can verify. Deleting it risked silently breaking orchestrator sizing (the mechanism PROJECT.md §11's 2026-09-01 mypy-OOM entry names as the intended fix) on a claim I could not check. Left as REVIEW.md's own open item | Housekeeping that closes real gaps (a wrong assumption about hook order, a gate whose own error message pointed at the wrong fix, a script fragile to the exact failure mode it just hit) without touching §6's hypotheses, §5's metrics, or the G3 block itself |

## 11. Open questions

- Induction objective: synthetic repeated-random, natural-text induction positions, or KL-to-$B$? Synthetic is cleanest but risks a circuit that only works on synthetic input; KL-to-$B$ makes $B$ a training signal and partly undercuts §1.2. **Unresolved and consequential — settle before G3.** Whichever is chosen, evaluate on the other as a held-out check.
- Head selection rule at $A$ — must be stated and applied before seeing results, not chosen afterward.
- ~~Local dev venv runs Python 3.12, so `mypy src` cannot be run locally at all~~ **Half resolved, 2026-09-01 (§10).** The venv is now 3.11, matching pyproject and CI, so the numpy-stub syntax failure is gone. `mypy src` still cannot run on this box, but for a different and unrelated reason: it is **OOM-killed** at ~1.1 GB RSS on t4g.small's 1.8 GB (`oom-kill … task=python … anon-rss:1140000kB`, 2026-09-01). So the mypy gate still must be verified via CI, not pre-flight — but the fix is now an instance size (`RVM_INSTANCE_TYPE` in `infra/rvm.env`), not a missing interpreter. (Real CI *did* catch two genuine `no-any-return` errors in the first `algebra.py` push — mypy strict itself is fine.)
- ~~**The same 3.11-vs-3.12 gap also breaks `tests/unit/test_metric_hash.py`**~~ **Resolved, 2026-09-01 (§10).** Confirmed to have been exactly the diagnosis recorded here — `ast.dump()`'s output differing between 3.11 and 3.12, not a real metric change. Under the 3.11 venv the test passes with `metric_hash.json` untouched, which is the independent confirmation the earlier byte-for-byte diff argued for. The standing warning still holds and is now permanent: **never regenerate `metric_hash.json` from a non-3.11 interpreter** — `--update` would bake that interpreter's AST-dump shape in as the baseline and break the check for CI and every other machine.
- Does the matched-$\beta$ unstructured control fix its random subspace before training or resample it? Fixed is the right analogue to LoRA.
- Is the antisym parameterization's companion term $-vu^\top$ actually benign? The prediction assumes the $+$ version interferes more. Untested.
- Cross-seed *pretraining* variance band: needed for any "X misses the mechanism" claim, expensive, currently deferred. Do not make claims requiring it.
- Identity-matrix oracles are transpose-blind: `copying_score(I, I, I) == 1.0` passes even under a `W_U`/`M_OV` argument-order bug, since `I.T == I`. Closed here with an independent per-token recomputation as a second oracle. `lora.py` and `models.py` will hit the same trap testing composed forms against identity/permutation weights — plan for a non-self-transpose oracle alongside any identity-based one.
- `lora.py`'s symmetric/antisymmetric deltas are square (`d x d`, matching `M_QK`/`M_OV`), but the "one matrix per form" rule (§3) says the *trained* object is `Delta W_Q` (rectangular, `d x d_head`) with `W_K` frozen — so `Delta M_QK = Delta W_Q W_K^T` is generically neither symmetric nor antisymmetric no matter how `Delta W_Q` is parameterized, since its row space is constrained to lie in `W_K`'s column space. train.py (○, not yet built) will need to decide how M3's triad actually gets *installed* into the model under this constraint: either (a) solve for the `Delta W_Q` whose composition with frozen `W_K` best matches the target square delta (a projection, not exact for arbitrary targets), or (b) inject the square delta directly via a forward hook on the attention-score computation itself, which respects "touch only one logical piece" in spirit (no `Delta W_Q Delta W_K^T` cross term) without being a literal weight edit to `W_Q`. Undecided — flagging now so M3 doesn't silently pick one without it being a visible choice.
- **`wrapper.sh`'s "close out the row" step is not actually triggered by worker completion.** Per CLAUDE.md's CD section, `wrapper.sh` is supposed to wait for all workers to terminate and then re-invoke Claude Code to close the row — but in practice the 7 G0 workers finished, pushed their shard commits, and self-terminated, and the row then sat at stale ⏳ (fully-collected data, gate never actually evaluated) for ~17.5 hours until a manual session noticed and ran `scripts/close_g0.py`. Worth checking whether `wrapper.sh`'s polling loop is actually being kept alive on a real EC2 orchestrator instance across the whole worker run, or whether it only works when someone is present to launch it and then the box gets stopped/disconnected before the wait-and-close step executes. Until that's confirmed, don't assume a sweep row is done just because its workers finished — check for the results record.
- **Nothing was reading CI.** CI went red at 5d88030 and stayed red through f35a28a; the failure was a tier-0 mypy error, which skips every downstream tier, so the repo went ~a day with no test having run on CI at all. CLAUDE.md's CD loop is explicitly gated on green (`wrapper.sh` step 4, "proceed only if green... Red CI blocks progress. It is a gate, not a parallel signal logged and ignored") — yet a session started, worked, pushed, and ended in that window. This is the same failure shape as the `wrapper.sh` "close out the row" bullet above: a step that only runs when a human is present. Check whether `wrapper.sh`'s `gh run list` wait is actually executing on the orchestrator, or whether sessions are being started outside it. Until confirmed, **check CI status by hand at the start of a session** rather than assuming the last push was green.
- **h0_4's separation band needs sign-off before M4/M6 report against it.** PROJECT.md §6 falsifies h0_4 by "separation exceeding the matched-norm random-null band" but does not say what the band is *of*. `nulls.phi_separation_null_band` makes it the distribution of $|\varphi_{QK} - \varphi_{OV}|$ over paired matched-norm null draws, at the 95th percentile — reusing the convention G1 and h0_6 already use, with no new threshold invented. That is the faithful reading, but it is a reading; see REVIEW.md.
- **G1's overlap-significance margin is narrow: 0.387 observed vs. 0.360 null 95th percentile (margin 0.027, ~7% relative).** The gate passed under the pre-registered criterion, but this is not a comfortable pass — a different null seed or a slightly different percentile choice (90th vs. 95th) could plausibly flip it. Since G1 is a one-time gate (not something to re-run/re-threshold post-hoc per CLAUDE.md's falsification discipline), this is not being re-litigated, but M1 (QK rank sweep) should be read with this in mind: if the QK arm struggles to reach criterion at low rank, this narrow prerequisite margin is a real alternative explanation to a capacity limit, not just h0₂'s optimization-limit story.
- This session's coding sandbox has ~1.8GB RAM with no swap — insufficient to load `pythia-70m` end to end (torch+transformers imports alone use ~700MB before the model; the HF→TL weight-conversion step OOM-killed the process every time with no swap). Worked around with a temporary 3GB swapfile (`fallocate`/`mkswap`/`swapon`, removed after); `tests/integration/test_models.py` then passed (2/2). No `gh workflow run` access from this session's PAT (403, insufficient scope) so `tier3-integration` couldn't be triggered as a fallback — the swapfile workaround was the only path to a definitive local read. Future sessions hitting the same OOM on an integration test should reach for temporary swap rather than assuming the code is broken. Recurred for G1's single-checkpoint forward pass (`scripts/run_g1.py`): even with `return_type=None` (skipping the unembedding matmul, since G1 only needs attention patterns), loading + one forward pass over checkpoint A still needed the same temporary-swapfile workaround on this box. A checkpoint load alone appears to be right at this box's ceiling regardless of what the forward pass computes.
- **Training (not just loading/forward-pass) on this box needs both swap *and* a bounded eval batch size, and a fresh boot does not come with swap active by default.** This session hit the sandbox's memory ceiling twice more, in new ways, closing G2: (1) a fresh boot had no swap active at all despite a `/swapfile_g2` file existing on disk from a prior session (never re-`swapon`'d — `swapon --show` was empty; the kernel OOM-killed training processes outright, confirmed via `journalctl -k`); (2) even with swap active, `indbw.train.compute_recovery`'s default `batch_size=32` materializes a ~1.65GB full-vocab logits tensor per batch at `T=128`/`d_vocab=50304`, which alone exceeds this box's RAM and swap-thrashes the process into an unrecoverable D-state hang rather than a clean OOM kill (harder to diagnose — the process just looks stuck). Both are now fixed at the code level (`train.py`/`run_g2.py` pass bounded `batch_size`, §10) but the swap-not-active-on-boot issue is an **infra** gap, not a code one: any future worker instance (or a restarted version of this one) needs swap set up as part of its bootstrap, not assumed persistent across reboots. `infra/worker-bootstrap.sh` should be checked for whether it does this already before the next training row runs unattended.
- **G3 failed with a training curve showing no real learning signal (loss flat 12.8-13.0 across all 400 steps, R crept 0.004→0.014), and this was not diagnosed further before stopping the phase (by design — CLAUDE.md "do not route around" a failed gate).** Two live hypotheses, undistinguished: (a) `lr=3e-3` is simply too low for this parameterization/optimizer combination at rank 64, or (b) something in the QK hook injection path (`train.py`'s `qk_hooks`) doesn't actually let the loss respond to `B, A` the way the existing unit tests (which only exercise `qk_hooks`' *forward*-pass correctness and gradient *existence*, never a real multi-step convergence check on the QK arm specifically — `test_overfitting_a_single_batch_reduces_loss` deliberately uses OV, per its own docstring, precisely because QK-only was already known to be hard even on a toy model) would catch. Before trusting G3's verdict as informative about §3's composition rule (as opposed to just this run's tuning), the next session should either (i) run a small lr sweep on the same generous-rank QK arm, cheap relative to a full G2-style run since it doesn't need 400 steps to see whether the loss moves at all in the first 20-40, or (ii) add the toy-model closed-loop convergence test for the QK arm specifically that `test_train.py` currently only has for OV, to rule out (b) directly. REVIEW.md flags this same ambiguity for human judgment.
- **UPDATE (same day, after the lr diagnostic sweep above): hypothesis (b) ("objective/loop broken") is now disfavored; a $W_K$-bottleneck hypothesis is favored instead, untested.** The lr sweep shows real, coherent, lr-dependent learning at every tested lr, ruling out a fully broken gradient path. But every lr and both tested step counts (100 and 400) plateau at $R \approx 0.01$–$0.015$ — nowhere near 0.80, and clearly saturating rather than climbing. A concrete structural explanation this suggests, not yet tested: **the QK arm only trains $\Delta W_Q$, so $\Delta M_{QK} = \Delta W_Q W_K^\top$'s row space is constrained to lie in $W_K$'s own column space at checkpoint $A$ (§11's earlier, still-unresolved note on this exact constraint) — and G1 already found $W_K$'s overlap with the prev-token subspace at $A$ to be a *narrow* pass (0.387 vs. a 0.360 null, margin 0.027).** If $W_K$ hasn't yet developed enough structure to support strong prefix matching, no rank of $\Delta W_Q$ can compensate, however large — the ceiling wouldn't be about rank at all, it would be about which matrix is frozen. This would tie G1's already-flagged narrow margin directly to G3's failure, rather than the two being independent findings. **Not tested — this is a candidate explanation, not a confirmed one; testing it (e.g., a positive control that also allows $W_K$ to adapt, purely diagnostic, not a change to §3's protocol) is a human call, since it would mean interpreting §8's "single-matrix composition rule too restrictive" branch as being about *which* matrix rather than about rank.** Also untested: whether many more steps (1000+) at the best-found lr (1e-2) breaks the plateau — the 100-vs-400-step comparison argues against it but doesn't rule it out definitively (§10, 2026-08-14).
- **UPDATE (2026-08-15, user-authorized diagnostic): the $W_K$-bottleneck hypothesis above is now disfavored.** Letting $W_K$ adapt too (generous rank on both $W_Q$ and $W_K$, same head/checkpoint/lr/objective, 150 steps) produced $R = 0.0051$ — no better than QK-only's 0.0117 — with a loss curve just as flat and noisy as every QK-only attempt (`data/g3_wk_bottleneck_diag.jsonl`, §10). If a frozen-$W_K$ ceiling were the real constraint, unfreezing it should have shown at least some early upward movement in $R$ even short of criterion; it showed none. **Three candidate explanations for G3's plateau are now ruled out or disfavored (broken objective/loop, insufficient rank, frozen-$W_K$ ceiling), and none has been confirmed as the true cause.** A new, untested candidate this raises: the fixed target head (layer 3/head 6, chosen from checkpoint $B$'s argmax-PMS head per the 2026-08-13 decision in §10) may simply be the wrong head to adapt *from $A$* — $B$'s winning head is not necessarily the one gradient descent would recruit starting from a checkpoint where no head has any PMS structure yet (§2's definition of $A$). Untested and not pursued further this session; flagging for the next human decision on where to look, per CLAUDE.md's reservation of new hypotheses to humans.
- **UPDATE (2026-09-03): the reachability graft (§10) localizes the plateau outside block 3 entirely, and disfavors optimization as the explanation across the board.** `qk_q` = B's real $W_Q$ for head (3,6), grafted with zero training, gives $R=-0.0006$ — an upper bound on every trained $\Delta W_Q$ from the six optimization diagnostics above, all of which plateaued near the same ~0.01 floor as the whole-head (0.0046) and whole-host-block (0.0077) graft cells. That converges five negative optimization diagnostics and one constructive graft on the same conclusion from two directions. **The open question this raises, not present before:** the graft that does work is `full` (1.0004), and nothing between `layer_host` and `full` was measured, so *where outside block 3 the missing structure lives* is unlocalized. Candidate next diagnostic (untested, needs human sign-off, §9): bisect the complement of block 3 — candidates include the embedding/unembedding matrices, LayerNorm affine parameters (all layers), and layers 0–2 / 4–5 individually — using the same `graft()` cell mechanism `scripts/diagnose_g3_reachability.py` already has, just with new cell definitions. This is a new test and an agent may not decide to run it.
- **NEW (2026-09-03): the prereq A/B contrast (§10) finds the previous-token prerequisite 2.6–15× weaker at $A$ than at $B$, on the same head both times — a candidate mechanism for the graft finding above, since layer 2 sits upstream of everything the graft diagnostics varied.** Untested: whether closing that specific gap (e.g., grafting only layer 2's prev-token head from $B$ into $A$, leaving block 3 untouched) moves $R$ — this would be a sharp test of "layer-2 prerequisite is the bottleneck" as a standalone claim, distinct from the broader localization bisection above, and is itself a new test needing sign-off.
- **NEW (2026-09-03): three layer-3 heads show prefix matching at $B$ — (3,6) 0.9652, (3,1) 0.8993, (3,0) 0.5094 — against ~0.006 at $A$ for all three, while §4's protocol adapts only one head.** If induction at $B$ is genuinely carried by three heads jointly, "the minimum-dimension update that installs prefix matching" under a single-head protocol may not be a low-rank version of what $B$ does — it may be answering a question about a different, smaller object than the one that actually exists at $B$. Whether M1–M8 should be rescoped to a multi-head arm, run single-head with this caveat attached to every result, or left as specified is a human call; nothing in §6 currently anticipates it.
- **PROPOSED (2026-09-04, not yet authorized): a training-free route through M1/M2/M5 that reuses the existing pipeline almost unchanged, contingent on the localization bisection above landing somewhere tractable.** The reachability graft's core move — evaluate a real, known-working weight substitution with zero training, rather than trying to reach one by gradient descent — is exactly what M5 (§7) already names as "the strongest available necessity evidence... evidence about the object rather than about the optimizer," except M5 as specified still starts from a *trained* generous-rank $\Delta W^\star$ before truncating it. Eight diagnostics now agree that training from $A$ does not move $R$ off a ~0.01 floor at any rank/lr/step budget tried — so the training step no longer produces a $\Delta W^\star$ worth truncating; the real, already-available $B - A$ delta can stand in for it directly. Concretely, once (and only once) the localization bisection above identifies a component set $C$ outside block 3 where grafting reaches criterion:
  1. Compute $\Delta W = W_B - W_A$ restricted to $C$ — no training, pure subtraction.
  2. `algebra.truncate_svd(ΔW, r)` for $r$ in §8's existing rank ladder $\{1,2,4,8,16,32\}$ — same numbers M1/M2 already specify, no new threshold invented.
  3. Extend `diagnose_g3_reachability.py`'s `graft()` to install the truncated delta instead of the full one; evaluate with the same `probes.py` functions the script already calls (`prefix_matching_score`, `icl_score`, `recovery`), same eval set, same `METRIC_VERSION` — no new metric.
  4. `nulls.matched_norm_random_update` at matched rank/norm in the same slot, as the §8-mandated control (M6) — confirms success comes from $B$'s real structure, not from any perturbation of that magnitude landing there.
  5. `algebra.phi` / `sym_antisym_split` on the localized, rank-truncated $\Delta M_{QK}$ and $\Delta M_{OV}$ answers H2 directly, with no training step anywhere in the chain.
  6. `sweep.py`'s `CellConfig`/run-ID hashing, `completed_run_ids`/`pending_cells` resumability, and `check_failure_claims` gate all apply unchanged — the only thing that changes is what a cell's executor does (truncate-and-graft instead of train-a-LoRA-adapter).
  This does not touch §6's hypotheses — H1's question ("is there a rank $r^\ast$ at which $R(r)$ tracks the singular-value spectrum") is unchanged, only the method for producing $R(r)$ changes from a training outcome to a direct construction. But it **is** a change to M1/M2/M5's operative method and needs explicit sign-off before any row runs under it (CLAUDE.md §9 reserves protocol decisions to humans, not just hypotheses) — and it inherits the single-head-vs-three-heads question above, since localizing and truncating around one head answers a different question if $B$'s real object is a 3-head circuit. **Sequencing: localization bisection first, this second** — truncating inside a component set already known not to suffice (e.g. `layer_host` alone) would just re-measure the existing 0.0077 floor.
  **UPDATE (2026-09-05): the bisection ran (§10) and its precondition — "identifies a component set $C$ outside block 3 where grafting reaches criterion" — is not yet met.** The best partial cell (`layer_host_plus_pre`, blocks 0-3) reaches $R = 0.1010$, an order of magnitude below the $0.80$ bar, despite reaching PMS $= 0.8949$. This proposal stays un-executable as written until either `ln_final` (unmeasured, six spot attempts failed) or an untested combination reaches criterion — see the new entry below for what the bisection actually showed instead.
- **NEW (2026-09-05): the localization bisection dissociates "prefix matching reachable by grafting" from "recovery reachable by grafting" — the first direct measurement of the §4 lumped-metric concern rather than an argument for it, and it changes what the next diagnostic should target.** Full numbers and provenance in §10. Summary: grafting the layer-2 prev-token head alone already crosses §5's PMS$\,\ge 0.3$ bar (PMS $0.0212 \to 0.3682$); adding all of blocks 0-2 to block 3 reaches PMS $0.8949$, next to `full`'s $0.9641$ — prefix matching is close to fully reachable by grafting a contiguous prefix of the network. **R does not follow**, reaching only $0.1010$ in that same cell, even though block 3's own $W_V,W_O$ for head (3,6) are $B$'s real weights throughout. Two things this does not resolve, both flagged rather than guessed at: (1) whether the missing $R$ lives in `ln_final` (unmeasured — six spot attempts on `t4g.medium` failed to land it, dropped for now per user direction, PROJECT.md §10 2026-09-05) or in a combination this bisection didn't test (every cell adds one component set to `layer_host` independently; nothing combines two or more of {block2/pre, post, embed_unembed, ln_final} together, and nothing tests "everything except one piece" the way `layer_host`-vs-`full` does at the coarse end); (2) whether a component-wise graft can even in principle close this gap, versus $A$ and $B$ occupying representational regimes that only compose wholesale (the `layer_host`-to-`full` jump itself, still unexplained). Deciding the next cell set here is explicitly a new-test design question (CLAUDE.md §9) — not this entry's to make.
- **UPDATE (2026-08-15, user-directed diagnostic): the wrong-target-head hypothesis above is now disfavored.** Three alternative heads $(2,1)$ prev-token head, $(3,0)$ same layer, $(4,6)$ one layer deeper — tested at the identical lr/rank/step budget as layer 3/head 6's own best short-run cell — all plateaued *lower* than layer 3/head 6's $R=0.0095$: 0.0035, 0.0016, 0.0030 respectively (`data/g3_head_diag_worker{0,1,2}.jsonl`, §10). Layer 3/head 6 is the best-performing of the four heads tested, not an arbitrary or poor choice. **Four candidate explanations for G3's plateau are now ruled out or disfavored (broken objective/loop, insufficient rank, frozen-$W_K$ ceiling, wrong target head), and none has been confirmed as the true cause.** Remaining untested candidates: the synthetic repeated-random objective itself (§11's induction-objective decision, 2026-08-14) may simply be a harder optimization target than natural-text induction positions; many more steps (1000+) at the best-found lr might still break the plateau (flagged as untested in the 2026-08-14 UPDATE entry above and still untested); or this may, after four negative diagnostics, be read as converging evidence toward a genuine capacity/structural limit under this parameterization — which is the §8 "single-matrix composition rule too restrictive" branch, not an optimization failure. That reading is a human call, not one this session is authorized to make (CLAUDE.md reserves falsification verdicts to humans; §6's thresholds and criteria are fixed, not something an agent concludes its way into).
- **UPDATE (2026-08-15, user-approved diagnostic): the full-fine-tune sanity-ceiling run is done, and it changes the shape of the question rather than closing it.** Unfreezing *every* parameter (LayerNorm and embeddings included — deliberately outside the M1–M8 protocol, diagnostic only) and training from $A$ on the identical objective produced $R=0.0022$ (lr=1e-4, 38/80 steps) and $R=0.0047$ (lr=1e-5, 53/80 steps) — both *below* QK-only G3's own $R=0.0117$, despite training loss falling substantially in both runs toward (not past) the ≈10.83 chance-level NLL (`data/g3_full_ft_worker{0,1}.jsonl`, §10). **This is the strongest evidence yet against a composition-rule capacity limit**: there is no "one matrix frozen" constraint left to blame when the whole model is trainable, and it still didn't move the needle. **Five candidate explanations for G3's plateau are now ruled out or disfavored (broken objective/loop, insufficient rank, frozen-$W_K$ ceiling, wrong target head, and now the single-matrix composition rule itself).** What has *not* been tested by any diagnostic so far, and is the most natural remaining candidate: the step budget and/or the synthetic repeated-random objective's intrinsic difficulty from checkpoint $A$ — every diagnostic to date has used $\le 400$ steps, and this one was itself truncated at 38–53/80 by the 30-minute wall-clock budget with loss still trending down. A genuinely informative next test would need either (i) an order-of-magnitude more steps than anything tried so far at the full-fine-tune setting, on a box that can sustain it, or (ii) direct comparison against the natural-text induction objective §11 already flags as untested. Also unexplained and worth a second look before further compute is spent: **$R$ declined monotonically over training in both full-fine-tune runs** (worker1: 0.00686→0.00472 over steps 10–50; worker0: 0.00253→0.00225 over steps 10–30) even while training loss fell — loss improvement is not translating into eval-set ICL advantage, and marginally moves the wrong way. Whether that is a real regression or noise at these tiny $R$ magnitudes is itself an open question. Which of these to pursue, and whether five negative diagnostics plus one that actively undercuts the composition-rule explanation should now be written up as "no evidence found for a capacity limit under this protocol, cause of G3's plateau still unidentified" rather than continuing to chase individual hypotheses, is a human call (CLAUDE.md reserves falsification verdicts to humans).
- **Minor coordination slip during the head-choice diagnostic (2026-08-15), no data corruption resulted:** while a local background job was partway through its 3-cell loop, an EC2 worker was launched for the one cell (layer 4/head 6) not yet started locally, to parallelize the tail of the run per the user's request to use workers. The local background job's already-queued loop then also reached that same cell before it could be redirected, so it ran a second time locally. The EC2 worker was terminated as soon as the local result landed and before the worker's own push step ran, so no duplicate or conflicting commit resulted — but it was closer than it should have been, and the EC2 spend on that cell was wasted. If splitting a small already-running local loop across local+remote execution again, kill (or pass a scoped `--worker-id` subset to) the local job *first*, before launching any EC2 worker for the same cells, rather than trying to race a redirect against an already-queued loop.
- **A derivation-side extension now exists (`docs/mathematics.md` §12–§18) separating "does a low-rank update exist" from "does training find one", and it bears on the standing G3 question above. It is mathematics, not protocol: no hypothesis, null, or threshold in §6 is added, altered, or re-thresholded by any of it, and nothing in it is a falsification test.** Five consequences worth a human read, in descending order of how much they change what a future row means:
  1. **The rank constraint was vacuous in every G3-family run.** $\Delta W_Q \in \mathbb{R}^{d \times d_h}$, so $\operatorname{rank}(\Delta W_Q) \le d_h = 64$ *unconditionally* (Lemma 13.2) — at $r = 64$ the LoRA image is all of $\mathbb{R}^{512\times 64}$. G3's record already labels its cell $r = d_{\text{head}} = 64$; the consequence is that no G3-family result can be read as a rank or capacity finding, because no rank constraint was in force. The informative sweep range for M1/M2 is $1 \le r < 64$.
  2. **Existence has never been evaluated, and does not need the training loop.** A head's attention pattern does not feed back into its own layer's input, so with only that head's $W_Q$ adapted the layer-$\ell$ inputs at $A$ are *exactly* unchanged (Obs 16.1). PMS is then an exact function of frozen activations, and "does any $\Delta W_Q$ install prefix matching at margin $\Delta$ from $A$" is a linear program (Thm 16.2, Cor 16.3) — feasible returns the witness update, infeasible returns a Farkas certificate that holds *independently of optimizer, budget, init and parameterization*, which is the necessity evidence §7 says a failed training run cannot supply. Not implemented (needs an LP solver dependency and one recorded activation set); whether to build it is a human call.
  3. **M1/M2's interpretability as a capacity measurement has a forward-measurable precondition.** The exact QK score gradient is $\partial L/\partial s_{ij} = \alpha_{ij}\langle g_i, z_j - \bar z_i\rangle$ (Thm 17.1), so it vanishes identically when the head's OV write is position-independent, and is bounded by that head's value spread (Cor 17.2) — a property of the head at $A$, independent of $r$. Prop 17.5: rank cannot repair a vanishing gradient, since every rank's gradient is a linear image of the full one. **A rank sweep run in a gradient-gated regime returns $R(r)$ flat at every $r$ — the literal statement of $h0_1$, by a mechanism that has nothing to do with intrinsic dimension.** The two quantities that would settle whether that regime obtains — the candidate head's value spread and its copying score at $A$ — are forward-only, and `probes.copying_score` already computes the latter. **No record in `results/` reports a copying score at $A$ for head (3,6), or for any head.** Whether to measure them is a human call; this is a derived dependency, not a proposed test.
  4. **G1's narrow overlap margin is a norm cost, not a feasibility barrier.** $\|\kappa_t\| = \|W_K^\top p_t\|$ enters the required update scale in the denominator (Prop 15.4), so overlap $0.387$ means roughly $2.6\times$ the update norm a perfectly-aligned $W_K$ would need — a hard ceiling only if the overlap were exactly zero. This agrees with, rather than merely coexisting with, the 2026-08-15 diagnostic that unfroze $W_K$ and found the same plateau.
  5. **H3 gains a derived direction without gaining a criterion.** Under isotropic codes the sufficient rank scales as $r \gtrsim 2\log m$ in the number of competitors (Thm 15.2), and QK competes against context positions ($m = 2T = 256$) while OV competes against the vocabulary ($m = |\mathcal{V}| = 50{,}304$), giving $r^\ast_{OV}/r^\ast_{QK} \approx \log|\mathcal{V}|/\log 2T \approx 1.95$ (Cor 15.3). **H3's null and criterion are unchanged** — it pre-registers only $r^\ast_{QK} \ne r^\ast_{OV}$, with no direction, and this does not amend it. A reversed ordering in data would be informative about the derivation's assumptions A1–A4, not a violation of anything pre-registered.
  The load-bearing weakness is that §15's assumptions (A1–A4, about the code geometry at $A$) are stated and not measured, so §15's existence claim is conditional. §16's LP is immune to that and is the reason to prefer it. Closed forms implemented in `indbw.existence` (18 unit tests, tier 1, no model loading); §16's LP deliberately not implemented. See REVIEW.md.

- **UPDATE (2026-08-19): the seven G3 diagnostics have all varied *optimization* axes; nobody has yet checked whether the QK arm's criterion is reachable in principle.** Every diagnostic so far (lr, rank, frozen-$W_K$, target head, full fine-tune, objective/steps) asks "can gradient descent find the update?" None asks the prior question: **does a $\Delta W_Q$ that satisfies $R \ge 0.80$ exist at all?** Two specific reasons to doubt it does, neither tested:
  - **$R$ is a lumped metric being used as a single-component gate.** §4 states plainly that "a lumped metric cannot tell you which was the bottleneck" and assigns each circuit component its own readout — prev-token score, PMS, copying score. But §8's G3 and §6's H1 then score the *QK-only* arm with $R$, which is built from ICL, which requires prefix matching **and** copying to both be present. If head (3,6)'s OV/copying circuit is absent at $A$ — never measured; `probes.copying_score` has only ever run on toy fixtures — then a $\Delta W_Q$ that installs *perfect* prefix matching still moves ICL, and therefore $R$, by approximately nothing. Under that reading G3 did not fail; it measured the copying half while adapting the prefix-matching half, and M1's rank sweep (same arm, same criterion) would fail identically at every rank for the same reason. **This is an internal inconsistency between §4 and §6/§8, not a result** — per this file's preamble it needs resolving explicitly rather than leaving both standing, and which side gives way is a human call (§6's thresholds are pre-registered and not agent-editable).
  - **The criterion's arithmetic is extreme.** With ICL($A$) $= -0.025$ and ICL($B$) $= 11.51$, $R \ge 0.80$ demands ICL $\ge 9.20$ nats, i.e. second-copy NLL a full 9.2 nats below first-copy NLL. Chance on uniform-random tokens is $\ln 50304 = 10.83$. So the criterion is very nearly "copy perfectly", with no partial credit for a working-but-imperfect induction head — even though §5's own induction-bearing threshold is PMS $\ge 0.3$, a far weaker bar. Note also that $R = 1.0$ corresponds to a second-copy NLL of $-0.68$ nats, which is unattainable; the scale is only reachable at all because ICL($B$) was measured with first-copy NLL above chance.
  - **A cheap, decisive test now exists and has not been run:** `scripts/diagnose_g3_reachability.py` grafts checkpoint $B$'s own weights into $A$ one circuit component at a time (`W_Q` only / `W_Q`+`W_K` / `W_V`+`W_O` / whole head / host block / everything) and reports $R$, PMS, and the NLL halves separately for each. $B$'s weights are a known-good witness, so the `qk_q` cell is an upper bound on what *any* $\Delta W_Q$ can reach from $A$: if grafting $B$'s own $W_Q$ does not move $R$, no rank and no step budget can, and the plateau is a criterion problem rather than an optimization one. If it *does* move $R$, the reverse follows and the compute-budget reading below is the live one. Costs a handful of forward passes, no training. 17 unit tests cover the graft's weight surgery (which head slice changed, which did not) and the eval plumbing; it needs HuggingFace access, which the session that wrote it did not have.
- **UPDATE (2026-08-19): the A/B training-budget accounting, which was never written down, is `scripts/analyze_training_budget.py` (+ `data/training_budget_accounting.json`).** Pythia-70m trains on $1024 \times 2048 = 2{,}097{,}152$ tokens per step, so $A$ (step 512) to the *next grid point* (step 1000) is 488 steps $= 1.02$B tokens $\approx 4.3 \times 10^{17}$ FLOPs at $6ND$; $A \to B$ is 3.12B tokens. G2/G3's run was 400 steps $\times$ batch 4 $\times$ seq_len 256 $= 409{,}600$ tokens $\approx 1.7 \times 10^{14}$ FLOPs. **Ratios: $2{,}499\times$ fewer tokens than the transition bracket, $7{,}619\times$ fewer than $A \to B$, and $2{,}048\times$ fewer tokens per optimizer step.** Two caveats keep this from being a bound in either direction: Pythia's tokens buy general LM improvement of which induction is a small fraction (§1.1), so a directly-aimed objective should be far more token-efficient; but conversely G3 trains 36,864 parameters from a checkpoint that already has the prerequisite (G1). What the number does settle is *scale* — G3 was short by three orders of magnitude, not by a factor of two — which is enough to say "more steps" was never ruled out by the 100-vs-400-step comparison the 2026-08-14 entry rested on. Closing that gap at G2's measured 5.5 s/step and batch 4 is ~1,526 CPU-hours, which is the arithmetic that makes G2's "CPU affordable" decision conditional on a 400-step budget that is now itself in question.
- **The full-fine-tune diagnostic's unexplained "$R$ declines while loss falls" (2026-08-15) has a candidate mechanism worth checking before more compute is spent.** Training loss is second-copy NLL alone (`train.second_copy_nll`), and on uniform-random tokens that loss has a large, easy, non-induction descent direction: flattening the output distribution toward the $\ln 50304 = 10.83$ uniform floor. The full-FT curves fall $13.27 \to 11.18$ — i.e. *toward* 10.83, not past it — which is exactly the signature of learning the uniform prior rather than learning to copy. Flattening lowers first- and second-copy NLL equally, so it moves ICL, and therefore $R$, by zero. That would explain both why the full fine-tune's loss moved a lot while $R$ did not, and why QK-only's loss stayed flat (a single head's $\Delta W_Q$ cannot flatten the output distribution, so the dominant descent direction is unavailable to it). **Untested, and cheap to test:** log `first_copy_nll` alongside the training loss — currently only the second half is recorded, so the two cannot be told apart in any existing run's `loss_history`. Nothing here is a proposal to change §5's metrics or `train.py`'s objective; it is a candidate explanation for an already-logged anomaly.
- **Two of this file's own infrastructure bullets are resolved, and one dev-tooling hazard is new.** The 3.11-vs-3.12 bullets above (mypy unrunnable locally; `test_committed_lockfile_matches_current_metric_source` false-positive) are environment-specific, as suspected: on a Python 3.11 box both pass clean — `python scripts/check_metric_hash.py` → "metric-hash check passed", and `mypy --python-executable "$(which python3)" src/indbw` → "Success: no issues found in 13 source files". The `--python-executable` flag is load-bearing on any box where packages live in `dist-packages` rather than `site-packages`: without it mypy reports 22 spurious `import-not-found` errors for numpy/torch/transformer_lens *and* 4 downstream errors that are artifacts of numpy resolving to `Any`. Separately: `ruff` and `mypy` were unpinned in `pyproject.toml`'s `[dev]` extra while CI's tier 0 runs both as a gate, so a new ruff release turns the build red on untouched code — it already had, with 12 pre-existing `E402`s on the `sys.path`-insert-then-import pattern that CI would have hit on the next push regardless of what that push changed. Now pinned (`ruff==0.15.8`) with per-file `E402`/`E731` ignores listed file by file rather than blanket-ignored, so a genuinely misplaced import in a new file still fails.

- **UPDATE (2026-08-19): the retrain-from-$A$ segment fits a 10GB RTX 3080 inside a 15-hour budget, and the CPU path does not.** `scripts/bench_retrain_throughput.py` measures achieved throughput on the target card (pythia-70m-shaped GPTNeoX built from config, no checkpoint download) and extrapolates. Projections at 59.5 TFLOP/s bf16 peak — GA102 *consumer* rate with FP32 accumulate, which is what torch AMP uses; the widely-quoted 119 TFLOPS is the FP16-accumulate rate and sizing against it halves every MFU figure:

  | segment | tokens | @20% MFU | @30% MFU |
  |---|---|---|---|
  | 512 → 1000 (contains the transition) | 1.02B | 8.2 h | 5.5 h |
  | 512 → 1100 | 1.23B | 9.9 h | 6.6 h |
  | 512 → 1500 | 2.07B | 16.6 h | 11.1 h |

  VRAM is not the binding constraint: fp32 master weights + fp32 grads + Adam moments + a bf16 compute copy is 1.18 GB, leaving the logits tensor as the only large allocation. micro-batch 8 at seq 2048 peaks near 6.9 GB with a naive `cross_entropy`; a chunked/fused CE drops that to ~2.5 GB and allows micro-batch 32. Pythia's 1024-sequence batch is then reached by gradient accumulation (×128 at micro-batch 8). Data slice for 512 → 1100 is 2.47 GB as uint16; a checkpoint is 282 MB of fp32 weights.

  **Do not run the retrain through TransformerLens.** `HookedTransformer` exists to expose every intermediate through a hook, which is what makes this repo's probes possible and what makes it the wrong vehicle for a multi-hour training run — train with plain HF `GPTNeoXForCausalLM` and load the resulting weights into TransformerLens for probing afterwards.

- **The `6ND` convention overestimates pythia-70m's per-token cost by ~23%, and in a way that hides which kernel matters.** `analyze_training_budget.per_token_flops` splits it: at $d_{\text{model}}=512$, seq 2048, vocab 50304, the weight matmuls are only **33%** of a token's forward+backward cost. The **unembedding projection alone is 45%**, because $d_{\text{model}}$ is small against the vocab; and attention's $\text{seq}^2$ term — which $6ND$ ignores entirely — is another **22%**, because $d_{\text{model}}$ is small against the sequence. $6N_{\text{total}}D$ gives $4.22\times10^8$ FLOPs/token against a detailed count of $3.43\times10^8$. Every *ratio* in the A/B accounting above is unaffected (both sides use the same formula), so the 2,499× figure stands; the absolute FLOP figures in the 2026-08-19 budget entry are the $6ND$ convention and are ~23% high. Flagged rather than silently restated, since the two conventions will otherwise be compared across sessions.

- **UPDATE (2026-08-21): the retrain is measured, not projected, and it is comfortably affordable — 53% MFU on an RTX 3080.** `scripts/bench_retrain_throughput.py` on the target card: **91,819 tok/s at micro-batch 4, seq 2048, bf16 + `torch.compile`, 4.19 GB peak VRAM.** Against a corrected causal-attention FLOP count that is 28.0 TFLOP/s achieved = **47% of the 59.5 TFLOP/s bf16 peak** (the script first reported 53%, before `per_token_flops` was corrected for causal masking — see the entry below). Either figure is far above the 15–35% band the 2026-08-19 sizing entry assumed, and it makes every candidate segment fit inside a 15-hour budget:

  | segment | steps | tokens | wall-clock |
  |---|---|---|---|
  | 512 → 1000 (contains the transition) | 488 | 1.02B | 3.10 h |
  | 512 → 1500 | 988 | 2.07B | 6.27 h |
  | **512 → 2000 (ends at published checkpoint $B$)** | **1488** | **3.12B** | **9.44 h** |

  **Running to 2000 is worth the extra 6 hours.** It is the only available check on whether cold-starting Adam at $A$ (Pythia publishes weights, not optimizer moments) perturbed the trajectory enough to matter: the re-run's endpoint can be compared directly against Pythia's published $B$ on PMS, ICL, and the weights themselves. Without that comparison every downstream claim rests on an unverified assumption about the re-run's fidelity.

  Revisions to the 2026-08-19 sizing entry, from measurement: micro-batch 8 **OOMs** on a 10GB card (4.19 GB observed at micro-batch 4 extrapolates to ~8.4 GB, plus fragmentation and whatever the desktop holds), so the estimate of 6.9 GB there was low. But the **chunked cross-entropy that entry recommended is not needed** — at 53% MFU there is little headroom left to buy, and micro-batch 4 with gradient accumulation ×256 (22.8 s per optimizer step) is sufficient. Drop it as a requirement.

  Two caveats on the measured figure, in opposite directions and roughly cancelling: the benchmark reuses one resident batch, so it excludes host→device transfer and data loading (real throughput lower unless overlapped with a prefetching pinned-memory loader); but it also runs `clip_grad_norm_` and `opt.step()` every micro-step, whereas accumulation ×256 amortizes those over 256 micro-steps (real throughput higher by ~2–5%). Treat 91,819 tok/s as accurate to about ±10%.

- **A benchmark that falls back to CPU is a silent failure, and this one did.** The first run of `bench_retrain_throughput.py` on the target box reported `device: cpu` and produced a fully-formatted result — tokens/s, MFU, wall-clock projections, fits/over verdicts — that was wrong by three orders of magnitude, because the venv was not active and torch could not see the GPU. Nothing in the output distinguished it from a real measurement except one parenthetical. The script now **refuses** unless `--allow-cpu` is passed explicitly, prints an install diagnostic on refusal, and has a `--doctor` mode that reports what torch sees of the GPU and exits. Separately, `torch.compile` failures (a torch/triton version mismatch raises on *import*, before any kernel is generated) now degrade to eager with a warning rather than taking down the run — eager is a valid measurement, it just understates throughput, so the result is a floor.

- **Correction to the FLOP model, same day: `per_token_flops` was counting the full $\text{seq}\times\text{seq}$ attention matrix.** A decoder attends only to positions $\le$ its own, so each query touches $\text{seq}/2$ keys on average — and this is not bookkeeping, since SDPA and FlashAttention with `is_causal` *skip* the upper triangle rather than computing and masking it. The term was 2× too large. Corrected split at pythia-70m's shape: body matmuls **37%**, attention **12%** (was 22%), unembedding **51%** (was 45%). Total drops from $3.43\times10^8$ to $3.06\times10^8$ FLOPs/token, which makes $6N_{\text{total}}D$ **38%** high rather than 23%. **This moves only MFU and achieved-FLOP/s; every wall-clock projection is extrapolated from measured tokens/s and never passes through a FLOP count, so the retrain timings above are unaffected.** The measured MFU restates as 47%, not 53%. Flagged as a correction rather than silently restated, since 53% was quoted once already and the two will otherwise be compared across sessions.

- **UPDATE (2026-08-21): the retrain harness is built (`src/indbw/retrain.py` + `scripts/run_retrain.py`), tested offline, and unrun.** One command: `python scripts/run_retrain.py --from-step 512 --to-step 2000`. Resumable (state saved every `--save-every` steps, re-running the same command picks up), refuses to start on CPU, writes `probe.jsonl` / `ckpt/` / `latest.pt` / `config.json` / `final/` under a run directory named by the config hash.

  **The design decisions worth knowing, since each is a way the run could produce a plausible-looking wrong answer:**

  1. **The LR schedule continues Pythia's, it does not start a new one.** Step 512 is *inside* Pythia's 1430-step warmup, at $3.58\times10^{-4}$ — 2.8× below the $10^{-3}$ peak, and still climbing through the entire 512→1000 transition window. A harness that starts at peak LR, which is what nearly every training script does by default, would run the window under study at almost three times the intended rate and the resulting transition would be about the harness, not about Pythia. `lr_at_step` takes Pythia's *global* step and is tested against the closed form at machine precision.
  2. **Gradient accumulation carries the $1/N$.** At `--grad-accum 256` the classic omission multiplies the effective LR by 256 while producing an entirely ordinary-looking loss curve. Pinned by an equivalence oracle against a single large batch, plus a discrimination guard asserting that the unscaled version really is inflated by exactly $N$ — so the oracle cannot go blind.
  3. **A rolling checkpoint buffer, not a save-on-trigger.** Onset is only detectable after it has happened, so the pre-onset weights must already be on disk when the probe notices. `RollingCheckpointBuffer` keeps the last `--buffer-steps` (24) and, once PMS crosses `--onset-pms` (0.05), retains everything for `--capture-steps` (60) more. Triggering is idempotent, because PMS is estimated on a finite eval set and will wobble across any threshold. A save-on-trigger design captures only the post-onset side — the side the delta is *not* measured against.
  4. **Probing reads the HF model directly, no TransformerLens in the training path.** `probe_induction` pulls attention patterns via `output_attentions` and reuses `probes.prefix_matching_score` and `train.first/second_copy_nll` unchanged, so what is logged every 4 steps is exactly §5's PMS and ICL on exactly the fixed eval set the committed G0/G1/G2 records used. It **raises** rather than returning a number when attentions come back empty: a PMS of 0.0 from a broken probe and a PMS of 0.0 from a pre-induction model are the same value in a log, and the whole experiment is the shape of that curve over time.

  **Not verified, and the first thing to check before committing 9 hours:** `HFStreamSource` has never seen a real dataset — the session that built it had no HuggingFace access, so the packing logic is covered only against a fake iterator and the dataset name, field name, and streaming behaviour are unverified. Decode the first batch before starting. Relatedly, the harness does **not** reproduce Pythia's batch order; that would need the pre-shuffled pretokenized Pile and an index into it, and was judged not worth the setup because Adam is cold-started at `from_step` regardless, so the trajectory diverges from the published one on the first step either way. This is the reason `--to-step 2000` is the default: step 2000 is checkpoint $B$, so ending there makes that divergence *measurable* against published weights rather than assumed away.

  Tested offline end to end (450 tests, tier 0–2.5 green): a `tests/smoke/test_retrain_smoke.py` runs load → schedule → accumulate → step → probe → buffer → resume on a randomly-initialized 2-layer model in under a second, so integration breakage surfaces in tier 2 rather than nine hours into a real run. What no test here covers is the real checkpoint, the real data, and the GPU.

---

## 12. Reading

- Elhage et al. 2021 — QK/OV circuits, K-composition; the framework §3 is written in
- Olsson et al. 2022 — induction heads, prefix-matching and copying scores
- Li et al. 2018; Aghajanyan et al. 2020 — intrinsic dimension of task updates; the correct framing for §1.1
- Hu et al. 2021 — LoRA
- Kalajdzievski 2023 — $\sqrt r$ scaling; bears on the §8 $\alpha/r$ confound
- Biderman et al. 2023 — Pythia suite, checkpoint structure, seed variants
- Huang, Jin, Li et al. 2025 (arXiv 2502.09858) — automated falsification; see §9 for what is taken and what is left
