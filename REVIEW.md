# Review queue

Append-only. Anything an "how this could be wrong" commit note couldn't
resolve lands here for human eyes (CLAUDE.md, "Adversarial self-review").

There is no reviewer in the CD loop. This is the substitute, and it is
weak — keep this queue short and actually read it.

| Date | Commit | What the note couldn't resolve |
|---|---|---|
| 2026-08-14 | (this commit) | **What counts as the "matched-lr arm" in `check_failure_claims`.** CLAUDE.md requires a failing rank cell to report "the matched-lr arm and the 10×-steps arm alongside it". The 10×-steps arm is unambiguous (h0₂). The matched-lr arm is not: with lr already swept independently per rank, I read it as *that sweep* — a rank may be called a capacity failure only if ≥3 distinct lr points at that rank all failed. That is a strictly stronger control than a single run at some reference lr, and it is what the grid already pays for. But it is a different object from what "the matched-lr arm" most literally names (one run at the lr that worked for the generous-rank reference), and if the intent was the literal reading, the gate as written would pass a claim that the literal reading would reject: three lr points clustered in a bad region all failing is not the same evidence as failing at the lr known to work. Confirm the reading before any M1/M2 failure is written up. |
| 2026-08-14 | (this commit) | **h0_3's matched-β control is a coordinate subspace, not a dense random one.** `nulls.matched_beta_support` pins all but β randomly-chosen *entries* of Δ*W* to zero. This matches β exactly and is fixed before training (§11's stated leaning), and the dense alternative is infeasible at these shapes (~9.7 GB, and β exceeds the ambient dimension at generous rank). But a coordinate subspace is not distributionally the same object as a dense Gaussian one: it is axis-aligned in the basis the weights happen to be stored in, so a Δ*W* whose useful directions align with coordinate axes is favoured, and one whose do not is penalised. If h0_3 comes back "the unstructured control performs identically", that conclusion is about *this* control. Nothing in the tests can distinguish "low-rank structure doesn't matter" from "the coordinate basis happened to be adequate". A human should confirm this is the intended control before M3/M6 run. |
| 2026-08-14 | (this commit) | **h0_4's separation band is a reading of §6, not a quotation of it.** §6 says h0_4 is falsified by "separation exceeding the matched-norm random-null band" without defining the band. `nulls.phi_separation_null_band` takes it as the 95th percentile of $\|\varphi_{QK} - \varphi_{OV}\|$ over *paired* independent matched-norm nulls — one draw per arm, each with its own shape and norm. The percentile reuses the repo's existing convention (G1, h0_6) rather than inventing one, and no threshold was edited. The alternative reading — two separate one-arm bands, asking whether the arms' bands overlap — is a different and generally *less* powerful test, and I cannot tell from §6 which was meant. Sign off before any M4/M6 row reports against it. |
| 2026-08-14 | 5d88030 | `train.py`'s LoRA hook injection (`qk_hooks`/`ov_hooks`) assumes TransformerLens's within-block hook execution order is stable (`ln1.hook_normalized` before `attn.hook_q`; `attn.hook_z` before `hook_attn_out`) so a "capture, then add" closure sees the right tensor. If a future TL version changed that order, the capture box would read a stale/mismatched tensor. The zero-init discrimination test (bit-identical forward pass at B=0) would not catch this, since it never depends on capture timing; only the nonzero-delta test would, and only if the reordering also broke shape compatibility rather than just changing values. No test currently pins the hook *order* itself — worth a direct assertion on TransformerLens's hook-firing sequence if this becomes a live risk (e.g. after any TL version bump). |
| 2026-08-14 | 7d87384 | G3 failed (R=0.0117 vs. 0.80) with a loss curve that never moved (flat 12.8–13.0 across all 400 steps) — logged as an *inconclusive* gate failure rather than a finding about PROJECT.md §3's composition rule, because flat-from-step-1 loss looks like an optimization/tuning problem (bad lr, or a subtler bug in the QK path) rather than genuine capacity exhaustion. This commit's note could not resolve which. `tests/unit/test_train.py`'s only convergence-mechanics test (`test_overfitting_a_single_batch_reduces_loss`) deliberately exercises the OV arm, not QK — its own docstring already flags QK-only as known-hard on a toy model, but there is no equivalent closed-loop test proving QK-only *can* converge under any hyperparameters, toy or real. Needs a human call: run an lr sweep on the same generous-rank QK config before trusting this G3 result, or add the missing QK convergence test to rule out a real bug first (PROJECT.md §11 has the same flag). |
| 2026-08-14 | (this commit) | **Follow-up to the row above: the lr sweep it asked for is done (6 lr values, 100 steps each, `data/g3_lr_diag_worker{0-5}.jsonl`) and updates but doesn't resolve the question.** "Broken objective/loop" is now disfavored — every lr shows real, coherent, lr-dependent learning (monotonic R rise at low-moderate lr, classic too-high-lr flattening at the top of the range). But all six, plus G2/G3's own 400-step run at lr=3e-3, plateau at $R \approx 0.01$–$0.015$, two orders of magnitude below the 0.80 criterion, with visibly shrinking increments (a saturating curve). PROJECT.md §11 now proposes a specific, untested structural explanation: the QK arm only adapts $\Delta W_Q$, so $\Delta M_{QK}$'s achievable row space is capped by $W_K$'s own column space at checkpoint A — and G1 already found that overlap to be a narrow pass (margin 0.027 vs. a random-subspace null). If true, no rank of $\Delta W_Q$ could ever close the gap, and G3's failure would really be a downstream consequence of G1's narrow margin, not an independent capacity finding about rank. **This note can't resolve it either** — confirming it needs a purpose-built diagnostic (e.g., a positive control that also lets $W_K$ adapt) that nothing in this session ran. A human call is needed on whether that's worth running, and if it confirms, how to phrase the resulting claim (PROJECT.md §7's necessity caveats apply directly: this would be evidence about *this* checkpoint's $W_K$, not a general claim that QK-only composition can't work). |
| 2026-08-15 | (this commit) | **Follow-up to the row above, run with explicit user authorization: the $W_K$-adapt diagnostic is done and disfavors the hypothesis it was meant to test.** `data/g3_wk_bottleneck_diag.jsonl`: generous rank on both $W_Q$ and $W_K$ at the same head/checkpoint/lr, 150 steps, $R = 0.0051$ — not better than QK-only's 0.0117, loss curve equally flat. This note's residual uncertainty: a *negative* diagnostic result is weaker evidence than a positive one would have been, since it can't distinguish "the $W_K$-bottleneck hypothesis is wrong" from "the diagnostic itself under-powers the test" (150 steps, one head, one lr, one seed — the same class of caveat §7 raises for any single failed training run). `qk_both_hooks` (new, `src/indbw/train.py`) has 5 unit tests mirroring `qk_hooks`' existing guards, so a hook-plumbing bug is less likely than a genuine null result, but not ruled out to the same standard a pre-registered M-phase row would require. PROJECT.md §11 now raises a new candidate (wrong target head for training-from-$A$, vs. $B$'s head) that this session did not pursue — needs the same kind of human sign-off before any further diagnostic spends more compute chasing it. |
| 2026-08-15 | ef4ece2 / (this commit) | **Follow-up to the row above, run at the user's direction: the head-choice diagnostic is done and also disfavors the hypothesis it tested.** `data/g3_head_diag_worker{0,1,2}.jsonl`: three alternative heads at lr=1e-2/rank=64/100 steps all plateau *below* layer 3/head 6's own $R=0.0095$ at the same budget (0.0035, 0.0016, 0.0030). This note's residual uncertainty is the same shape as the row above: three heads at one lr/seed/step-count each is a narrow probe, not an exhaustive search over all 48 heads x lr x seed — a head this session didn't try could still do better, and this can't rule that out, only make it less likely a priori given the trend. **After four negative diagnostics (broken objective, insufficient rank, frozen-$W_K$, wrong head), PROJECT.md §11 now explicitly asks whether this should be read as converging evidence toward a genuine capacity/structural limit** (the §8 "composition rule too restrictive" branch) rather than continuing to chase optimization explanations — flagged there as a human call, not concluded by this session. Also flagged in §11: a minor coordination slip running local + a launched EC2 worker on the same cell concurrently (no data corruption, but worth reading before repeating the local+remote split pattern). |
| 2026-08-15 | 99ae344 / (this commit) | **Follow-up to the row above, run with user approval: the full-fine-tune sanity-ceiling diagnostic is done, and its result cuts the other way from the previous four — it disfavors the composition-rule-limit explanation itself, not one of the alternatives to it.** `data/g3_full_ft_worker{0,1}.jsonl`: unfreezing the entire model (not just one matrix) still produced $R=0.0022$/$0.0047$, both *below* QK-only G3's $R=0.0117$. This note's residual uncertainty, not resolved here: (1) both runs were cut off by the 1800s wall-clock budget at 38/80 and 53/80 steps, with training loss still visibly decreasing at the cutoff — the diagnostic itself is under-powered relative to its own design, the same caveat class as the $W_K$-bottleneck row above, and more so here since even the *intended* 80-step budget was already short relative to G2/G3's 400-step run; (2) only two lr values were tried, chosen by the standard full-fine-tune-vs-LoRA convention (1-2 orders of magnitude below the LoRA lr) rather than swept, so an unlucky lr choice for full fine-tuning specifically cannot be ruled out; (3) $R$ *declined* over training in both runs while loss fell (worker1: 0.0069→0.0047 over 40 steps) — unexplained, and could be either a real signal (loss reduction on random training batches doesn't generalize to the fixed eval set's induction-specific gap) or eval noise at $R$ magnitudes this small; nothing in this run distinguishes the two. **Net effect on the standing question:** five negative diagnostics now share the unexplained floor, and the composition-rule branch itself is now the least-supported explanation among those tested, not the most likely one as the 2026-08-14 lr-diagnostic note originally suggested. Whether to keep spending compute chasing individual hypotheses (objective vs. checkpoint vs. step-budget) or to write this up as "G3's plateau is real and unexplained under this protocol" is a human call PROJECT.md §11 now poses directly. |
| 2026-08-19 | (this commit) | **The seventh G3 diagnostic is built but unrun, and the thing it tests is a contradiction inside PROJECT.md rather than a bug in the code.** §4 says a lumped metric cannot identify which circuit component is the bottleneck, and gives each component its own readout. §6/§8 then gate the QK-*only* arm on $R$, which is derived from ICL, which needs prefix matching **and** copying. If head (3,6)'s copying circuit is absent at $A$, the QK arm's criterion is unreachable by construction — and G3's failure, plus all six follow-up diagnostics, would be measuring that rather than anything about rank, lr, which matrix, which head, or step budget. `scripts/diagnose_g3_reachability.py` settles it by grafting $B$'s own weights into $A$ component by component, which bounds what any $\Delta W_Q$ can achieve without training anything; it has 17 unit tests on the weight surgery and eval plumbing but needs HuggingFace access, which this session's network policy blocked. **Two things need a human before this goes further.** (1) Whether §4 or §6/§8 gives way is a pre-registered-threshold question and explicitly not an agent's call — I have recorded the inconsistency in §11 and changed nothing in §6 or §8. (2) The graft is *suggestive, not a proof of impossibility*: $B$'s $W_Q$ is optimal in $B$'s network, not in $A$'s, so a low `qk_q` cell bounds nothing rigorously — it is strong evidence only in combination with the already-logged full-fine-tune result. Read it that way or not at all; §7's necessity caveats apply with full force. |
| 2026-08-19 | (this commit) | **The A/B compute accounting (`scripts/analyze_training_budget.py`) says G3 was three orders of magnitude short of the pretraining transition, and I cannot tell whether that matters.** 2,499x fewer tokens than $A$ → the next grid point, 2,048x fewer tokens per optimizer step. The honest reading is that it removes "more steps was already ruled out" as a defensible claim (the 100-vs-400-step comparison the 2026-08-14 entry rested on spans a 4x range inside a 2,499x gap, which is no evidence at all) — but it does **not** establish that more steps would help, because a directly-aimed objective should be far more token-efficient than incidental formation during general pretraining, and nothing here estimates by how much. The failure mode this note cannot rule out: the ratio is rhetorically forceful and numerically correct while being the wrong comparison entirely, if the real obstacle is the criterion-reachability issue in the row above. **Run the graft diagnostic before spending anything on the budget reading** — they are competing explanations and the cheap one discriminates. Relatedly, G2's "CPU affordable" verdict is conditional on the 400-step budget it timed; if the step budget moves by even 10x, that gate's decision needs re-taking (a §10 supersede, by a human, not a silent re-read). |
| 2026-08-20 | (this commit) | **The §12–§18 existence derivation rests on assumptions about the code geometry at $A$ that nobody has measured, and its most consequential claim is the one *least* protected by its tests.** `docs/mathematics.md` §15 constructs an explicit rank-$r$ update and derives $r \gtrsim 2\log m$ for it. Every closed form in it is unit-tested (`tests/unit/test_existence.py`, 18 tests, machine-precision where the mathematics is exact) — but the tests verify the *algebra*, not the *premises*. A1–A4 (a separable query-side token direction, a previous-token write with non-negligible $\|\kappa_t\|$, near-isotropic codes) are stated and unmeasured; if the discriminative directions sit in the tail of the key-code spectrum rather than its head, the construction still exists but its required norm blows up and the effective threshold rank rises well above $2\log m$. **A test suite that passes to 1e-12 on a derivation whose premises are unverified is exactly the shape of confident-wrong-number this queue exists to catch, and no test here can close it** — §16's LP is the fix (it interrogates the actual matrices instead of a model of them) and is deliberately not implemented, since it needs an LP-solver dependency this project does not carry and a recorded activation set from $A$. Human call: whether the LP is worth building, and whether §15's numbers may be cited before A1–A4 are measured. Second, smaller item: Cor 15.3 derives $r^\ast_{OV}/r^\ast_{QK} \approx 1.95$, giving H3 a *direction* it does not pre-register. §6's null and criterion are untouched and PROJECT.md §11 says so explicitly, but a derived directional expectation sitting next to an undirected pre-registered null is a live confirmation-bias surface when M2 eventually reports — confirm the framing before that row is written up. Third: Thm 17.1's vanishing-gradient result implies M1/M2 are uninterpretable as capacity measurements unless the candidate head's OV value spread and copying score at $A$ are nonzero, and neither has ever been measured for head (3,6) (or any head) despite `probes.copying_score` existing since the Setup phase. That is a derived dependency, not a proposed test — deciding whether to measure it is a human call under CLAUDE.md's falsification rules. |
| 2026-08-22 | (this merge) | **The merge of #5 into main's G3 work puts an analytic result and an already-built, unrun diagnostic on the same question, and they agree — which is worth acting on but is also exactly when a wrong premise gets waved through.** `docs/mathematics.md` §17 (Thm 17.1 / Prop 17.5) derives that the QK arm's gradient vanishes *identically, at every rank*, when the candidate head's OV write is position-independent, so a sweep run in that regime returns $R(r)$ flat in $r$ — h0₁'s literal statement, reached by a mechanism that has nothing to do with intrinsic dimension. Main's `scripts/diagnose_g3_reachability.py` (2026-08-19 rows above) independently argues the QK arm's criterion may be unreachable at $A$ because $R$ needs copying as well as prefix matching, and grafts $B$'s weights component-wise to find out. **These are the same concern from the analytic and empirical sides, and the second is the instrument for the first**: the graft's `ov_vo` and whole-head cells measure the premise §17's conclusion hangs on, so the "should head (3,6)'s copying score and value spread at $A$ be measured" call logged in the 2026-08-20 row above needs no new code — only HuggingFace access, which the writing session lacked. Two cautions before this convergence is read as corroboration. (1) It is not independent evidence: both are derivations from the same unmeasured premise about $A$, and neither is a measurement. Agreement between two arguments that share an assumption is not confirmation of the assumption. (2) The graft is suggestive, not a proof of impossibility (main's own note says so, and §7's necessity caveats apply), while §17 *is* exact — so if the graft comes back ambiguous, §17's conclusion does not inherit that ambiguity, and vice versa. Run the graft, read the two separately, and do not let the fact that they point the same way substitute for the measurement neither of them performed. |
| 2026-09-01 | (this commit) | **`data/retrain/cb25e3f6c2185c1e/provenance.json` names the wrong checkpoints as the onset capture, and the run is not reproducible across its own resumes.** `scripts/run_retrain.py:177-189` restores `buffer.triggered_at` from `latest.pt` on resume but never restores `buffer._retained`. Three consequences, all silent: eviction stops (the buffer cannot delete steps it does not know it wrote), `capture_exhausted` re-arms so each resume grants a fresh 84-checkpoint window, and `provenance.json`'s `retained_checkpoints` — written as `buffer.retained_steps()` at exit — describes only the final session. This run resumed twice (backwards step transitions in `probe.jsonl` at 868→852 and 1540→1528), so provenance advertises steps 1528-1860 while onset was at step 620. Anything reading `retained_checkpoints` to locate the pre/post-onset delta measures an arbitrary window ~900 steps past the transition: no crash, plausible numbers, wrong answer. `onset_bracket.json` (added this commit) records the real bracket, 528-852; `provenance.json` is deliberately left unmodified as a record of what the process emitted. **Three things this note cannot resolve.** (1) The eviction failure is the only reason the onset bracket still exists — had the buffer worked as designed across resumes it would have evicted the pre-onset side, which is precisely the half the harness exists to preserve. The correct fix is therefore not obvious: restoring `_retained` on resume makes eviction work again and would have *destroyed* this run's usable data. Whether the buffer should evict at all after a trigger, or whether resume should re-derive `_retained` from the directory listing rather than from `latest.pt`, is a design call. (2) Steps 856 and 860 sit inside the intended 84-checkpoint bracket but were overwritten by session 2's re-run of steps 853-868, and that re-run is a different trajectory — `probe.jsonl` shows different `train_loss` at identical steps across the two passes (852: 3.6353 vs 3.8482; 856: 3.6944 vs 3.7898), so the `hf-stream` position is not checkpointed and resume replays different batches from the same weights. Divergence is small (pms ≤ 0.007) but the seed-everything guarantee in CLAUDE.md does not hold across a resume, and no results record currently carries that caveat. I excluded 856/860 and called the clean bracket 82 checkpoints; whether a 58-checkpoint post-onset side is still adequate for D1 is a human call. (3) I have not verified that any checkpoint's *contents* are what the filename claims — the reconciliation is entirely from filenames, sizes (all exactly 281,731,463 bytes), and `probe.jsonl`. If `offer()`'s `.clone()` ever failed to detach a live `state_dict`, every file in a session would hold the same final weights and nothing here would detect it. A single load-and-diff of two adjacent checkpoints would settle it and has not been run. |
| 2026-09-01 | (this commit) | **The metric-hash gate's failure message tells you to do the one thing that would destroy what it protects.** Mostly a footnote to the §11 entry closed the same day by the 3.11 venv pin — the diagnosis there is correct and the fix is the right one. Two things that entry does not record. First, the mechanism, now pinned down exactly: `normalized_definitions` hashes `ast.dump(node)`, and 3.12 (PEP 695) emits `type_params=[]` on every `FunctionDef`/`ClassDef` where 3.11 emits nothing. Stripping `, type_params=[]` from the 3.12 dumps reproduces all 13 committed per-definition hashes bit-for-bit, which is stronger confirmation than the byte-for-byte source diff: it shows the divergence is entirely that one field, not merely that the sources are unchanged. Second, and the reason this is queued rather than dropped: the check's own failure text reads "Changing a metric definition invalidates every prior results record. Bump METRIC_VERSION in src/indbw/schema.py, then regenerate". §11 warns a *reader* never to regenerate off 3.11, but the warning lives in `PROJECT.md` while the instruction lives in the tool output, carrying the authority of the check, at the moment someone is trying to get a red test green. Following it bumps `METRIC_VERSION` — which tier 2.5 then surfaces as failures across every historical record, exactly as intended for a *real* metric change — and bakes a 3.12 lockfile that fails CI's 3.11, whose apparent remedy is another bump. The venv pin makes this unreachable on the fleet, not unreachable in general: this instance's own venv predates the pin and still reproduces it today. Suggested minimal change, not made here because it edits an integrity guard: have `check()` compare the running interpreter against the version that wrote the lockfile and, on mismatch, say the hash may be version-skewed and refuse to recommend a bump. Recording the generating Python version in `metric_hash.json` would make that check trivial and is a one-field addition. |
| 2026-09-01 | (this commit) | **Sweep workers now launch as one-time spot instead of on-demand (`infra/wrapper.sh`'s `run-instances` call), at the user's request, and this removes an implicit assumption the wait-loop relied on.** Before this change, "instance terminated" *implied* "worker pushed a result" — the only two things that ever ended a worker were a successful push (`shutdown -h now`) or a hard cap / push failure (`stop`, not terminate), so the loop's terminated-vs-stopped check was already a correct completion signal. A spot reclaim also terminates the instance (AWS forces terminate-on-interruption for one-time requests; stop/hibernate needs a *persistent* request, and persistent's own auto-restart runs no code — the worker's job is entirely in `--user-data`, which fires once at first boot — so a persistent request would come back up idle after a reclaim rather than resuming, trading this problem for a silent-zombie one). Under spot, "terminated" no longer implies "pushed," so the loop now also checks, per worker, for a `git log --grep`-matched `Worker <id> result` commit after the pull, and refuses to proceed (exit 1, naming the missing worker IDs) if any is absent — verifying the actual evidence of completion instead of trusting an instance-state proxy. **What this note can't resolve:** this is unexercised — no sweep has run under spot yet, so the check has not seen a real reclaim. Two things worth confirming once one does: (1) whether `research-vm-worker-template`'s subnet/AZ actually has t4g spot capacity reliably, since `run-instances` would simply fail to launch a worker at all if not (a different failure than reclaim, and the loop's current retry-lessness treats a launch failure and a silent hang the same way — worth a human look at whether that's adequate); (2) the grep's anchors assume `worker-bootstrap.sh`'s commit message text never changes — a future edit to that message without a matching edit here would make every worker look like it never pushed, which is a loud, not silent, failure (the loop exits 1) but still worth knowing if it happens. Also noted in passing: `infra/rvm.env`'s `RVM_INSTANCE_TYPE` is not read by either `wrapper.sh` or `worker-bootstrap.sh` — the actual worker instance type comes from the launch template (`research-vm-worker-template`, currently `t4g.small` in all 3 versions) — so that key appears to be dead config, a leftover from whatever `research-vm-infra`'s generic template originally described before this repo's `infra/` scripts hardcoded the values directly. Left unchanged; a human should decide whether to wire it up or delete it. |
| 2026-09-01 | (this commit) | **The 7th G3 diagnostic (`scripts/diagnose_g3_reachability.py`, the reachability graft flagged in the 2026-08-19/08-22 rows above as the discriminating experiment) is still unrun — its blocker changed from "no HuggingFace access" to "not enough memory on this box."** HuggingFace access now works from this instance (confirmed: `curl huggingface.co` → 200, and the script did successfully download and load checkpoint A's 76 tensors). It was then OOM-killed loading checkpoint B alongside it: `journalctl -k` shows two kill events, anon-rss 1.13–1.26 GB against this box's 1.8 GB total, the same magnitude and the same box (t4g.small) as the mypy OOM logged in §11/§10 for an unrelated reason. Loading two full TransformerLens-converted checkpoints simultaneously (plus the HF-format intermediate each conversion holds before discarding it) apparently costs more headroom than one checkpoint does. **This note can't resolve which fix is right.** Candidates, untried: (1) run on a larger instance, the same fix already applied to the mypy OOM via `infra/rvm.env`'s `RVM_INSTANCE_TYPE` — but no `rvm` CLI is available from this box to provision one, so this needs whoever operates the fleet; (2) load B, extract only the tensors each cell actually grafts, then free it before loading A, rather than holding both full models live — a real code change to a script CLAUDE.md already logged 17 unit tests against, not something to edit without sign-off; (3) load in a lower-precision dtype. No cell of the diagnostic ran; `data/g3_reachability_diag.jsonl` does not exist. Human call on which fix, before spending another attempt on this box. |
| 2026-09-01 | (this commit) | **Resolves item (3) of the onset-bracket row above; the other two stand.** That row flagged that the entire reconciliation was done without opening a single checkpoint, so a `.clone()` failure in `RollingCheckpointBuffer.offer()` would have left 82 copies of the same weights and every check would still have passed. Four checkpoints (528, 616, 620, 852) were pulled from S3 and loaded. Each is 76 tensors / 70,426,624 parameters, consistent with pythia-70m. **All 76 tensors differ across all 6 pairs — zero byte-identical tensors** — so the duplication mode is ruled out. Delta norms also scale sanely with step gap (‖ΔW‖_F = 2.91 / 35.30 / 103.49 at gaps of 4 / 88 / 324) with per-step drift *falling* (0.728 → 0.401 → 0.319), i.e. sublinear cumulative distance from partially-cancelling updates, which is what a real optimizer trajectory looks like and what duplicated or synthetic data would not reproduce. **Two limits on this, deliberately not oversold.** Only 4 of the 82 bracket checkpoints were opened, so this is a spot check, not a proof over the set — a corruption confined to checkpoints I did not sample would survive it. And steps 856/860 were *not* among the four, so the contamination claim that excludes them from the clean bracket still rests entirely on `probe.jsonl`'s differing `train_loss` across the two passes, not on any weight comparison; confirming it directly would mean re-running steps 853-860 from the 852 checkpoint, which nothing currently plans to do. Incidental observation worth recording: no tensor is frozen across the four, so this run trained every parameter including LayerNorm and embeddings. That is correct for this harness — it reproduces natural training A→B to observe onset — but it means this data is not produced under the M1-M8 protocol, and CLAUDE.md's freezing rule neither applies to it nor was violated by it. Anyone reaching for these checkpoints as if they were M1-M8 artifacts would be making a category error. |
| 2026-09-03 | (this commit) | **Spot worker launches cannot succeed in this account, and the check that was supposed to catch this passed anyway.** Commit ca3e20d moved sweep workers to spot in two places — `wrapper.sh`'s `--instance-market-options` flag *and* `research-vm-worker-template` version 4, which bakes the same spot options in and is the default version. Both fail at launch: `AuthFailure.ServiceLinkedRoleCreationNotPermitted` — `AWSServiceRoleForEC2Spot` does not exist in account 176048535722 and `research-vm-ssm-role` cannot create it. That row's own note said the spot path was "unexercised" and flagged capacity as the thing to confirm; the actual blocker is authorization and fires before any capacity question. **The trap worth recording is that `run-instances --dry-run` returns `DryRunOperation` ("would have succeeded") for the exact spot call that then fails.** Dry-run validates parameters and the caller's `ec2:RunInstances` permission, not service-linked-role creation, so a pre-flight built on `--dry-run` will greenlight a launch path that cannot work. I used dry-run to establish that workers were launchable at all and it told me spot was fine. Anything that adds a launch pre-flight to `wrapper.sh` should not rely on `--dry-run` alone. **Three things this note cannot resolve.** (1) The fix is one IAM call — `aws iam create-service-linked-role --aws-service-name spot.amazonaws.com` — which CLAUDE.md forbids me from making and which needs whoever holds fuller credentials. Until then every sweep launched through `wrapper.sh` fails at the first `run-instances`, loudly, before any worker boots. (2) I worked around it for this one job by pinning `--launch-template ...,Version=3` (the last version without market options) and launching on-demand. I did *not* change the template's default version or `wrapper.sh`'s flag, because reverting the user's explicit spot request without sign-off seemed worse than leaving it visibly broken and documented — but that means `main` currently contains a sweep path that cannot launch. Decide whether to create the SLR or revert ca3e20d; leaving both is the one option that should not persist. (3) Whether the on-demand fallback should be automatic. Making `wrapper.sh` catch this error and retry on-demand would keep sweeps running, but it silently converts a cost decision the user made deliberately into a more expensive one, which is exactly the class of thing that should not happen unattended. |
| 2026-09-03 | (this commit) | **The 7th G3 diagnostic ran and its result needs a human verdict I am not permitted to write.** `data/g3_reachability_diag.jsonl`, worker `g3reach`, 8/8 cells. Controls hold: `full` = 1.0004, `base_a` = -0.0002, and two internal consistency checks pass that no assertion in the script enforces — the `ov` graft leaves PMS bit-identical to `base_a` (0.0055; OV cannot affect attention patterns, so this is the right answer and a broken graft would not produce it), and `qk_qk` moves PMS strictly further than `qk_q` alone (0.0212 vs 0.0092). **`qk_q` = -0.0006.** That cell installs checkpoint B's real `W_Q` for head (3,6) — the exact tensor the QK arm trains — and is therefore an upper bound on what any `ΔW_Q` of any rank, at any lr, for any step budget, can reach from A. It moves nothing. On its face this is the discriminating result the 2026-08-19/08-22 rows called for: G3's plateau is not an optimization failure in the QK arm. **What I cannot resolve, and what makes this more than a clean answer.** (1) The result overshoots the question. Grafting all four matrices of the head gives 0.0046 and the entire host block gives 0.0077 — the same ~0.01 floor as every optimization diagnostic — while `full` gives 1.0004. So the structure that makes B's induction head *function* is not in the head, and not in its block, but is somewhere in the complement of block 3. Whether that reads as "the circuit is distributed", "A and B are not in the same basin so no graft composes", or something about LayerNorm/embedding scale co-adaptation is exactly the interpretive call CLAUDE.md reserves for humans, and the three have very different consequences for whether M1-M8 is well-posed at all. I have deliberately not marked the G3 row, appended a §10 decision, or touched §6. (2) The diagnostic cannot localize the gap between `layer_host` (0.0077) and `full` (1.0004) — nothing between those two was measured, and bisecting it would be a new test, which §9 forbids me from designing. (3) `full` and `base_b` both give R = 1.0004 rather than exactly 1: measured ICL 11.5170 against the `ICL_B = 11.5120` constant the script reuses so R stays on the same scale as prior diagnostics. A 0.04% gap, plausibly aarch64 float ordering, but it is the first time that constant has been checked against a re-measurement on this hardware and nothing currently asserts the two agree. If that drift is real rather than numerical, every R in every prior diagnostic carries the same small bias. (4) Ran on an on-demand `t4g.medium` (~$0.006) because spot cannot launch in this account — see the row above. Peak RSS was not captured: the worker booted `worker-bootstrap.sh` as of `e6acb6d`, one commit before the `/usr/bin/time -v` instrumentation landed, so the smallest instance that would have sufficed is still unmeasured and `t4g.medium` remains a guess rather than a sized choice. |
| 2026-09-03 | (this commit) | **Spot now works; the spot-*only* guardrail does not, and the gap is invisible from inside a dry-run.** Verified by real launch: `i-05b1e72a92e552e04` came up with `InstanceLifecycle: spot`, so `AWSServiceRoleForEC2Spot` exists and `ca3e20d`'s path is live — every sweep through `wrapper.sh` can launch again. Two of the three guardrails discussed are in place and one is not. **Confirmed working:** an instance-type restriction. `g5g.xlarge` (GPU) under spot now returns `UnauthorizedOperation`, so the no-GPU rule is enforced structurally rather than by my compliance with CLAUDE.md, which is the right place for it. **Confirmed missing:** the spot-only `Deny`. `run-instances` against launch-template `Version=3` with no market options still returns `DryRunOperation` — i.e. this role can still launch on-demand, exactly as I did earlier today. The template's default version carries spot but v1–v3 do not, so the escape hatch is a single CLI argument. `wrapper.sh`'s post-launch `InstanceLifecycle != "spot"` assertion (e000eda) catches it for sweeps launched through that script, but nothing constrains a direct `run-instances`, and I am the thing making direct calls. **What this note cannot resolve.** (1) Whether the missing `Deny` is deliberate — an operator may want an on-demand path retained for a case where spot capacity is unavailable and a run genuinely must complete. If so the current state is correct and this row is just documentation. If not, the policy from the 2026-09-03 discussion (`Deny ec2:RunInstances` when `ec2:InstanceMarketType != spot`) closes it, and is the only mechanism that holds regardless of template version or flags. (2) A verification asymmetry worth internalizing beyond this account: the *presence* of the type restriction was provable by dry-run, but the *presence* of spot capability was not — dry-run returned success for spot both before and after the service-linked role existed. Negative authorization results from `--dry-run` are trustworthy; positive ones are not, and any future pre-flight that treats `DryRunOperation` as "this will work" will be wrong in exactly the way that cost this project a session. |
| 2026-09-03 | (this commit) | **The prerequisite G1 certified at A is 2.6x weaker there than at B, and its margin over chance is 15x smaller. This is a candidate account of the entire G3 plateau, and it needs a human verdict I am not writing.** `data/prereq_ab_diag.jsonl`. Validation first: A's re-measured numbers reproduce the committed G1 record to three decimals (prev-token 0.3556 vs 0.356; overlap 0.3871 vs 0.387; null 0.3603 vs 0.360), which is the check that the grid, eval set, and hook wiring are right, since both were computed by the same imported code path. **The contrast G1 never took:** prev-token score 0.3556 -> 0.9403; overlap ratio 0.3871 -> 0.7691; overlap margin over the random-subspace null 0.0268 -> 0.4081. The head does not move — layer 2 head 1 at both checkpoints — so this is the same circuit component strengthening, not a different one appearing. G1's gate passed at A on both criteria and both passes were technicalities: 0.356 against a 0.3 bar, and a margin of 0.027 that §11 flagged at the time as "not a comfortable pass." At B the same statistics are not close to their thresholds at all. **Why this is more than a restatement of G1's caveat.** It supplies a single mechanism for results that have been accumulating as separate negatives: the deficit sits at layer 2, which is upstream of layer 3, which is why the 2026-09-03 graft of B's *entire layer-3 block* still left R at 0.0077; which is why the 2026-08-15 diagnostic that unfroze W_K did not help, W_K being in block 3; and which is why B's own W_Q does not transfer, being tuned to a 0.94-strength prev-token signal that A does not supply. Every G3 diagnostic varied something in or around layer 3. **Second observation, arguably as important and entirely unplanned:** the PMS grid at B shows *three* layer-3 heads doing prefix matching — (3,6) at 0.9652, (3,1) at 0.8993, (3,0) at 0.5094 — against a uniform ~0.006 at A. M1-M8 adapts a single head. If induction at B is carried by three, then a single-head protocol is not a low-rank version of what B does, it is a different object, and the "minimum dimension" being measured may not be the minimum dimension of the thing that actually exists. Nothing in §6 anticipates this and I have not touched it. **Four things this cannot resolve.** (1) The verdict. These are two checkpoints of Pythia's natural pretraining run, so the temptation is to say the prev-token head strengthens and induction follows — but that is a developmental claim, and CLAUDE.md reserves those for D1, which has not run. Correlation across two checkpoints is what this is; the causal ordering is not measured here and must not be asserted from it. (2) Whether the right response is to rescope M1-M8, to treat G1's threshold as mis-set (it was pre-registered; per §9 it must not be edited retroactively, only recorded as questioned), or to conclude the phase is well-posed and something else explains G3. All three are consistent with these numbers. (3) Only two checkpoints were measured. Where between 512 and 2000 the prev-token head strengthens is unmeasured, and the `data/retrain/` onset bracket (528-852) may already contain what is needed to say — that connection has not been checked. (4) Whether 0.3 was ever the right bar. Nothing in the repo derives it; if it came from convention rather than from this model, then G1 passing at 0.356 says less than its ● suggests, and every row downstream inherits that. |
| 2026-09-05 | (this commit) | **A one-off script's monolithic push design cost a full run to a spot reclaim, and the fix used (splitting cells across ten workers by hand) papers over it rather than fixing it.** `diagnose_g3_reachability.py` only reaches its `git push`/S3-sync step after every `--cells` argument finishes, so a single 10-cell job that gets reclaimed mid-run — which happened, at ~45 minutes in, with nothing recoverable — loses everything computed so far, contrary to CLAUDE.md's "results must be written incrementally... a reclaim costs one cell, not the run" (the script *does* flush each cell's JSON line to local disk immediately, but nothing ships it off-box until the whole command exits). The workaround here was operational, not a code fix: relaunch as ten separate one-cell spot workers, each with its own `--out` shard, merged by hand afterward. That worked (9/10 landed; the 10th failed six times regardless of cell count, so it wasn't purely a duration effect) but it is not a general solution — anyone rerunning or extending this script inherits the same fragility unless they also remember to shard by hand. **What this note cannot resolve:** whether to change the script itself (sync-and-optionally-push after every cell, matching `g0_sweep.py`'s `sync_to_s3`-per-checkpoint pattern) or leave it as a one-off diagnostic where hand-sharding is an acceptable cost each time it's rerun. Also unresolved: `layer_host_plus_ln_final` never landed after 6 spot attempts on `t4g.medium` while every other cell (identical shape, same two checkpoints, same eval) succeeded within 1-2 attempts — I cannot tell from outside the account whether this is coincidence (a run of bad luck concentrated on one cell) or something narrower (e.g. capacity contention specific to whatever AZ that particular launch happened to land in, given `$Default` launch-template version doesn't pin an AZ). Dropped per user direction; a human re-attempting it later should not assume the same six-in-a-row failure is diagnostic of anything about the *cell* itself. |
| 2026-09-05 | (this commit) | **The localization bisection (PROJECT.md §10/§11) needs the same kind of human verdict the 2026-09-03 rows above do, and for the same structural reason — a "no verdict" measurement sitting next to a proposed protocol change that depends on it.** The headline number: `layer_host_plus_pre` (blocks 0-3 grafted from B) reaches PMS $=0.8949$ — prefix matching essentially solved — while $R = 0.1010$, an order of magnitude under criterion, despite that cell already containing B's real OV weights for the target head. This is a cleaner dissociation than anything in the 2026-09-03 rows, because it isolates the *matching* half from the *recovery* half directly rather than inferring it from block-level grafts that always bundled both. What I have not done, and am not the one to decide: (1) design the next graft cell(s) — the natural candidates (combine `pre` with `post`, with `embed_unembed`, with the still-missing `ln_final`; or test whether *any* proper subset of the complement of block 3 reaches criterion, versus only the full complement working) are each a new test under CLAUDE.md §9's falsification discipline, not something this session's authorization ("run the six cells already coded") extends to. (2) update the §11 "training-free M1/M2/M5 route" proposal's status beyond "precondition not met" — whether that proposal should be abandoned, revised to not require a fully localized component set (e.g., truncate within `layer_host_plus_pre` despite its low R, if the point is comparing rank ladders rather than reaching criterion), or left exactly as specified pending further localization is a protocol call, not a data one. (3) `layer_host_plus_embed_unembed`'s `nll_first` of 17.14 (worse than chance, worse than `base_a`'s own 13.11) is flagged in §10 as "a decoder tuned to a different representation space" but that is a description of the number, not an explanation verified against anything — I have not checked, for instance, whether the LayerNorm scale mismatch between $A$'s residual stream and $B$'s unembedding (the two are jointly trained during pretraining, and this cell holds LayerNorm at $A$'s values) accounts for it, which would bear directly on whether `ln_final` — still unmeasured — is likely to matter a little or a lot. |
